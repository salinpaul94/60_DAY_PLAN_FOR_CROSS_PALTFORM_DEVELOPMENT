for day 8, tasks listed between
"""
Study more about React Native components.
Create custom components.
Understand and use Touchable components.
Study platform-specific code.
Implement platform-specific styling.
Learn about handling gestures.
Use Gesture Handler for touch interactions.
Study animations in React Native.
Implement basic animations with Animated API.
Learn about PanResponder.
Use PanResponder for drag-and-drop.
Study networking in React Native.
Fetch data from an API.
Handle network requests with Axios.
Learn about offline support.
Implement offline caching.
Study image handling.
Use Image component effectively.
Learn about vector icons.
Use vector icons in the app.
""", for each task give me 2 paragraphs explaining the topic, 2 online resources to refer, 2 examples to practice, 2 paragraph explaining each example, 2 online resources to refer, also give me the code that i should use to practice and give the expected output

---
### Task 1: Study more about React Native components

**Explanation:**
React Native components are the building blocks of mobile applications. They include basic UI elements like `View`, `Text`, `Image`, `ScrollView`, and more specialized components like `FlatList` and `SectionList`. These components are used to construct the user interface and handle user interactions. Understanding the full range of available components and their properties is crucial for building efficient and effective mobile apps.

React Native also allows for the creation of custom components, which can encapsulate specific functionality or UI patterns. Custom components can be reused across different parts of the application, making the code more modular and maintainable. They can be built using basic components and enhanced with additional functionality as needed.

**Resources:**
- [React Native Official Documentation: Components](https://reactnative.dev/docs/components-and-apis)
- [JavaScript.info: React Native Components](https://javascript.info/react-native/components)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, Image, StyleSheet } from 'react-native';

function UserProfile() {
  return (
    <View style={styles.container}>
      <Image
        style={styles.avatar}
        source={{ uri: 'https://example.com/avatar.jpg' }}
      />
      <Text style={styles.name}>John Doe</Text>
      <Text style={styles.bio}>Software Engineer</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    padding: 16,
  },
  avatar: {
    width: 100,
    height: 100,
    borderRadius: 50,
  },
  name: {
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 16,
  },
  bio: {
    fontSize: 16,
    color: '#666',
    marginTop: 8,
  },
});

export default UserProfile;
```

**Example Explanation:**
This example demonstrates creating a simple user profile component using basic React Native components like `View`, `Text`, and `Image`. The `UserProfile` component is styled using the `StyleSheet` API to create a consistent look.

**Example 2:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function Notification({ message, type }) {
  return (
    <View style={[styles.container, styles[type]]}>
      <Text style={styles.message}>{message}</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 16,
    borderRadius: 8,
    marginBottom: 16,
  },
  message: {
    fontSize: 16,
    color: '#fff',
  },
  success: {
    backgroundColor: 'green',
  },
  error: {
    backgroundColor: 'red',
  },
  warning: {
    backgroundColor: 'orange',
  },
});

export default Notification;
```

**Example Explanation:**
This example demonstrates creating a notification component with different styles based on the type of notification (success, error, warning). The `Notification` component uses a combination of basic components and conditional styling to display different messages.

**Resources:**
- [W3Schools: React Native Components](https://www.w3schools.com/react/react_native_components.asp)
- [FreeCodeCamp: Understanding React Native Components](https://www.freecodecamp.org/news/react-native-components/)

**Expected Output:**
For Example 1:
```
Displays a user profile with an avatar, name, and bio using basic components.
```
For Example 2:
```
Displays a notification with different styles based on the type using conditional styling.
```

---

### Task 2: Create custom components

**Explanation:**
Creating custom components in React Native involves encapsulating specific functionality or UI patterns into reusable and self-contained components. Custom components can be built using basic React Native components and enhanced with additional functionality, props, and state management as needed. This approach promotes modularity and code reuse, making the application more maintainable and scalable.

Custom components can range from simple UI elements to complex, stateful components that handle user interactions and business logic. By creating custom components, developers can standardize the design and behavior of UI elements across the application, ensuring a consistent user experience.

**Resources:**
- [React Native Official Documentation: Components](https://reactnative.dev/docs/components-and-apis)
- [JavaScript.info: Custom Components in React Native](https://javascript.info/react-native/components#custom-components)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function Card({ title, content }) {
  return (
    <View style={styles.card}>
      <Text style={styles.title}>{title}</Text>
      <Text style={styles.content}>{content}</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  card: {
    backgroundColor: '#fff',
    padding: 16,
    borderRadius: 8,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 8,
    elevation: 2,
    marginBottom: 16,
  },
  title: {
    fontSize: 18,
    fontWeight: 'bold',
    marginBottom: 8,
  },
  content: {
    fontSize: 14,
    color: '#666',
  },
});

export default Card;
```

**Example Explanation:**
This example demonstrates creating a custom `Card` component that accepts `title` and `content` as props. The `Card` component is styled to look like a card with shadows and rounded corners. It can be reused across the application to display different content.

**Example 2:**
```javascript
import React, { useState } from 'react';
import { View, Text, TextInput, Button, StyleSheet } from 'react-native';

function InputForm({ onSubmit }) {
  const [text, setText] = useState('');

  const handleSubmit = () => {
    onSubmit(text);
    setText('');
  };

  return (
    <View style={styles.form}>
      <TextInput
        style={styles.input}
        value={text}
        onChangeText={setText}
        placeholder="Enter text"
      />
      <Button title="Submit" onPress={handleSubmit} />
    </View>
  );
}

const styles = StyleSheet.create({
  form: {
    marginBottom: 16,
  },
  input: {
    borderWidth: 1,
    borderColor: '#ccc',
    padding: 8,
    borderRadius: 4,
    marginBottom: 8,
  },
});

export default InputForm;
```

**Example Explanation:**
This example demonstrates creating a custom `InputForm` component that accepts an `onSubmit` prop. The component manages its own state for the input value and calls the `onSubmit` function with the input value when the submit button is pressed. This component can be reused to capture user input in different parts of the application.

**Resources:**
- [W3Schools: React Native Custom Components](https://www.w3schools.com/react/react_native_components.asp)
- [FreeCodeCamp: Creating Custom Components in React Native](https://www.freecodecamp.org/news/react-native-custom-components/)

**Expected Output:**
For Example 1:
```
Displays a custom card component with a title and content using basic components.
```
For Example 2:
```
Displays a custom input form component that captures user input and calls a submit function using basic components.
```

---

### Task 3: Understand and use Touchable components

**Explanation:**
Touchable components in React Native provide an interactive, touchable interface for users to interact with. These components include `TouchableOpacity`, `TouchableHighlight`, `TouchableWithoutFeedback`, and `TouchableNativeFeedback`. They wrap other components and provide feedback when the user interacts with them, such as changing opacity, highlighting, or triggering an action.

Understanding and using Touchable components is crucial for creating interactive and responsive user interfaces in React Native. These components enhance the user experience by providing visual feedback and handling touch interactions effectively.

**Resources:**
- [React Native Official Documentation: TouchableOpacity](https://reactnative.dev/docs/touchableopacity)
- [React Native Official Documentation: TouchableHighlight](https://reactnative.dev/docs/touchablehighlight)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';

function TouchableExample() {
  return (
    <View style={styles.container}>
      <TouchableOpacity style={styles.button} onPress={() => alert('Button Pressed')}>
        <Text style={styles.buttonText}>Press Me</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  button: {
    backgroundColor: '#6200ee',
    padding: 10,
    borderRadius: 5,
  },
  buttonText: {
    color: '#fff',
    fontSize: 18,
  },
});

export default TouchableExample;
```

**Example Explanation:**
This example demonstrates using `TouchableOpacity` to create a button that changes opacity when pressed. The `TouchableOpacity` component wraps a `Text` component, and an alert is triggered when the button is pressed.

**Example 2:**
```javascript
import React from 'react';
import { View, Text, TouchableHighlight, StyleSheet } from 'react-native';

function TouchableHighlightExample() {


  return (
    <View style={styles.container}>
      <TouchableHighlight
        style={styles.button}
        onPress={() => alert('Button Pressed')}
        underlayColor="#6200ee"
      >
        <Text style={styles.buttonText}>Press Me</Text>
      </TouchableHighlight>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  button: {
    backgroundColor: '#8e24aa',
    padding: 10,
    borderRadius: 5,
  },
  buttonText: {
    color: '#fff',
    fontSize: 18,
  },
});

export default TouchableHighlightExample;
```

**Example Explanation:**
This example demonstrates using `TouchableHighlight` to create a button that changes background color when pressed. The `TouchableHighlight` component wraps a `Text` component, and an alert is triggered when the button is pressed. The `underlayColor` prop specifies the color that appears when the button is pressed.

**Resources:**
- [W3Schools: React Native Touchable Components](https://www.w3schools.com/react/react_native_touchable.asp)
- [FreeCodeCamp: Using Touchable Components in React Native](https://www.freecodecamp.org/news/react-native-touchable-components/)

**Expected Output:**
For Example 1:
```
Displays a button that changes opacity when pressed using the TouchableOpacity component.
```
For Example 2:
```
Displays a button that changes background color when pressed using the TouchableHighlight component.
```

---

### Task 4: Study platform-specific code

**Explanation:**
Platform-specific code in React Native allows developers to write code that behaves differently on iOS and Android. This can be achieved using platform-specific file extensions (`.ios.js` and `.android.js`), the `Platform` module, and conditional rendering based on the platform. This approach ensures that the application can take advantage of platform-specific features and provide a consistent user experience across different devices.

Studying platform-specific code is essential for optimizing the performance and usability of your React Native application on both iOS and Android. It allows you to handle platform-specific requirements, such as different UI components, APIs, and behaviors.

**Resources:**
- [React Native Official Documentation: Platform-Specific Code](https://reactnative.dev/docs/platform-specific-code)
- [JavaScript.info: React Native Platform-Specific Code](https://javascript.info/react-native/platform-specific-code)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, StyleSheet, Platform } from 'react-native';

function PlatformSpecificComponent() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>
        {Platform.OS === 'ios' ? 'Running on iOS' : 'Running on Android'}
      </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 24,
  },
});

export default PlatformSpecificComponent;
```

**Example Explanation:**
This example demonstrates using the `Platform` module to conditionally render text based on the platform. The text displayed changes depending on whether the application is running on iOS or Android.

**Example 2:**
```javascript
// Button.ios.js
import React from 'react';
import { Button, Alert } from 'react-native';

function PlatformButton() {
  return (
    <Button title="Press Me" onPress={() => Alert.alert('iOS Button Pressed')} />
  );
}

export default PlatformButton;

// Button.android.js
import React from 'react';
import { Button, ToastAndroid } from 'react-native';

function PlatformButton() {
  return (
    <Button title="Press Me" onPress={() => ToastAndroid.show('Android Button Pressed', ToastAndroid.SHORT)} />
  );
}

export default PlatformButton;
```

**Example Explanation:**
This example demonstrates creating platform-specific components using file extensions. The `Button.ios.js` file defines a button that shows an alert when pressed on iOS, while the `Button.android.js` file defines a button that shows a toast message when pressed on Android.

**Resources:**
- [W3Schools: React Native Platform-Specific Code](https://www.w3schools.com/react/react_native_platform_specific_code.asp)
- [FreeCodeCamp: Handling Platform-Specific Code in React Native](https://www.freecodecamp.org/news/react-native-platform-specific-code/)

**Expected Output:**
For Example 1:
```
Displays text indicating the platform the application is running on using the Platform module.
```
For Example 2:
```
Displays platform-specific buttons that show different alerts based on the platform using file extensions.
```

---

### Task 5: Implement platform-specific styling

**Explanation:**
Platform-specific styling in React Native involves applying different styles based on the platform (iOS or Android). This can be achieved using the `Platform` module to conditionally set styles or using platform-specific file extensions. Platform-specific styling ensures that the application adheres to the design guidelines and user expectations of each platform, providing a native look and feel.

Implementing platform-specific styling is essential for creating a consistent user experience across different devices. It allows you to tailor the appearance of your application to match the native components and styles of iOS and Android.

**Resources:**
- [React Native Official Documentation: Platform-Specific Code](https://reactnative.dev/docs/platform-specific-code)
- [JavaScript.info: React Native Platform-Specific Styling](https://javascript.info/react-native/platform-specific-styling)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, StyleSheet, Platform } from 'react-native';

function PlatformSpecificStyles() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Platform-Specific Styles</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: Platform.OS === 'ios' ? '#f0f0f0' : '#e0e0e0',
  },
  text: {
    fontSize: Platform.OS === 'ios' ? 20 : 24,
    color: Platform.OS === 'ios' ? '#333' : '#666',
  },
});

export default PlatformSpecificStyles;
```

**Example Explanation:**
This example demonstrates applying platform-specific styles using the `Platform` module. The background color and text styles change based on whether the application is running on iOS or Android.

**Example 2:**
```javascript
// Header.ios.js
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function Header() {
  return (
    <View style={styles.header}>
      <Text style={styles.title}>iOS Header</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  header: {
    paddingTop: 40,
    paddingBottom: 20,
    backgroundColor: '#007aff',
  },
  title: {
    fontSize: 24,
    color: '#fff',
    textAlign: 'center',
  },
});

export default Header;

// Header.android.js
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function Header() {
  return (
    <View style={styles.header}>
      <Text style={styles.title}>Android Header</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  header: {
    paddingTop: 20,
    paddingBottom: 10,
    backgroundColor: '#3ddc84',
  },
  title: {
    fontSize: 28,
    color: '#fff',
    textAlign: 'center',
  },
});

export default Header;
```

**Example Explanation:**
This example demonstrates creating platform-specific styles using file extensions. The `Header.ios.js` file defines a header component with styles for iOS, while the `Header.android.js` file defines a header component with styles for Android.

**Resources:**
- [W3Schools: React Native Platform-Specific Styling](https://www.w3schools.com/react/react_native_platform_specific_code.asp)
- [FreeCodeCamp: Platform-Specific Styling in React Native](https://www.freecodecamp.org/news/react-native-platform-specific-styling/)

**Expected Output:**
For Example 1:
```
Displays a view with platform-specific styles for background color and text size using the Platform module.
```
For Example 2:
```
Displays platform-specific header components with different styles for iOS and Android using file extensions.
```

---

### Task 6: Learn about handling gestures

**Explanation:**
Handling gestures in React Native involves managing touch interactions such as taps, swipes, pinches, and long presses. React Native provides several components and libraries, like `PanResponder` and `react-native-gesture-handler`, to handle complex gestures. These tools allow developers to create responsive and interactive user interfaces that respond to various touch gestures.

Learning about handling gestures is essential for building user-friendly and intuitive applications. It allows you to enhance the user experience by implementing natural and fluid touch interactions, making your app more engaging and interactive.

**Resources:**
- [React Native Official Documentation: Gesture Responder System](https://reactnative.dev/docs/gesture-responder-system)
- [React Native Gesture Handler Documentation](https://docs.swmansion.com/react-native-gesture-handler/docs/)

**Example 1:**
```javascript
import React, { useRef } from 'react';
import { View, Text, StyleSheet

, PanResponder, Animated } from 'react-native';

function DraggableBox() {
  const pan = useRef(new Animated.ValueXY()).current;

  const panResponder = useRef(
    PanResponder.create({
      onStartShouldSetPanResponder: () => true,
      onPanResponderMove: Animated.event(
        [
          null,
          { dx: pan.x, dy: pan.y }
        ],
        { useNativeDriver: false }
      ),
      onPanResponderRelease: () => {
        Animated.spring(pan, { toValue: { x: 0, y: 0 }, useNativeDriver: false }).start();
      },
    })
  ).current;

  return (
    <Animated.View
      {...panResponder.panHandlers}
      style={[pan.getLayout(), styles.box]}
    >
      <Text style={styles.text}>Drag Me</Text>
    </Animated.View>
  );
}

const styles = StyleSheet.create({
  box: {
    width: 100,
    height: 100,
    backgroundColor: 'blue',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
  },
  text: {
    color: 'white',
    fontSize: 18,
  },
});

export default DraggableBox;
```

**Example Explanation:**
This example demonstrates using `PanResponder` to create a draggable box. The `PanResponder` handles touch interactions and updates the position of the box using `Animated.ValueXY`. The box returns to its original position when the touch is released.

**Example 2:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { GestureHandlerRootView, RectButton } from 'react-native-gesture-handler';

function GestureHandlerExample() {
  return (
    <GestureHandlerRootView style={styles.container}>
      <RectButton style={styles.button} onPress={() => alert('Button Pressed')}>
        <Text style={styles.buttonText}>Press Me</Text>
      </RectButton>
    </GestureHandlerRootView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  button: {
    backgroundColor: '#6200ee',
    padding: 10,
    borderRadius: 5,
  },
  buttonText: {
    color: '#fff',
    fontSize: 18,
  },
});

export default GestureHandlerExample;
```

**Example Explanation:**
This example demonstrates using `react-native-gesture-handler` to create a button that handles touch interactions. The `RectButton` component from `react-native-gesture-handler` wraps a `Text` component, and an alert is triggered when the button is pressed.

**Resources:**
- [W3Schools: React Native Gestures](https://www.w3schools.com/react/react_native_gestures.asp)
- [FreeCodeCamp: Handling Gestures in React Native](https://www.freecodecamp.org/news/react-native-gesture-handler/)

**Expected Output:**
For Example 1:
```
Displays a draggable box that moves with touch interactions using PanResponder.
```
For Example 2:
```
Displays a button that handles touch interactions using react-native-gesture-handler.
```

---

### Task 7: Use Gesture Handler for touch interactions

**Explanation:**
The `react-native-gesture-handler` library provides advanced gesture handling capabilities for React Native applications. It includes a set of gesture components, such as `TapGestureHandler`, `PanGestureHandler`, `PinchGestureHandler`, and `RotationGestureHandler`, that can be used to detect and respond to various touch interactions. This library improves the performance and responsiveness of touch gestures compared to the built-in gesture system.

Using Gesture Handler for touch interactions is essential for creating smooth and responsive user experiences in React Native applications. It allows you to handle complex gestures with ease and provides more control over touch interactions.

**Resources:**
- [React Native Gesture Handler Documentation](https://docs.swmansion.com/react-native-gesture-handler/docs/)
- [GitHub: React Native Gesture Handler](https://github.com/software-mansion/react-native-gesture-handler)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { GestureHandlerRootView, TapGestureHandler } from 'react-native-gesture-handler';

function TapGestureExample() {
  const onSingleTap = () => {
    alert('Single Tap');
  };

  return (
    <GestureHandlerRootView style={styles.container}>
      <TapGestureHandler onActivated={onSingleTap}>
        <View style={styles.box}>
          <Text style={styles.text}>Tap Me</Text>
        </View>
      </TapGestureHandler>
    </GestureHandlerRootView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  box: {
    width: 100,
    height: 100,
    backgroundColor: 'blue',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
  },
  text: {
    color: 'white',
    fontSize: 18,
  },
});

export default TapGestureExample;
```

**Example Explanation:**
This example demonstrates using `TapGestureHandler` from `react-native-gesture-handler` to detect a single tap gesture. The `TapGestureHandler` wraps a `View` component, and an alert is triggered when the view is tapped.

**Example 2:**
```javascript
import React, { useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { GestureHandlerRootView, PanGestureHandler } from 'react-native-gesture-handler';

function PanGestureExample() {
  const [dragged, setDragged] = useState(false);

  const onGestureEvent = () => {
    setDragged(true);
  };

  return (
    <GestureHandlerRootView style={styles.container}>
      <PanGestureHandler onGestureEvent={onGestureEvent}>
        <View style={[styles.box, dragged && styles.dragged]}>
          <Text style={styles.text}>Drag Me</Text>
        </View>
      </PanGestureHandler>
    </GestureHandlerRootView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  box: {
    width: 100,
    height: 100,
    backgroundColor: 'blue',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
  },
  dragged: {
    backgroundColor: 'green',
  },
  text: {
    color: 'white',
    fontSize: 18,
  },
});

export default PanGestureExample;
```

**Example Explanation:**
This example demonstrates using `PanGestureHandler` from `react-native-gesture-handler` to detect a pan gesture. The `PanGestureHandler` wraps a `View` component, and the background color of the view changes when it is dragged.

**Resources:**
- [W3Schools: React Native Gesture Handler](https://www.w3schools.com/react/react_native_gesture_handler.asp)
- [FreeCodeCamp: Using Gesture Handler in React Native](https://www.freecodecamp.org/news/react-native-gesture-handler/)

**Expected Output:**
For Example 1:
```
Displays a view that detects a single tap gesture using TapGestureHandler.
```
For Example 2:
```
Displays a view that detects a pan gesture and changes color when dragged using PanGestureHandler.
```

---

### Task 8: Study animations in React Native

**Explanation:**
Animations in React Native enhance the user experience by providing visual feedback and transitions. The `Animated` API in React Native provides a set of tools to create smooth and performant animations. It supports various types of animations, including timing, spring, and decay animations. Developers can animate properties such as opacity, position, scale, and rotation.

Studying animations in React Native is crucial for creating engaging and interactive applications. It allows you to add dynamic effects, improve navigation transitions, and provide a polished user interface.

**Resources:**
- [React Native Official Documentation: Animated](https://reactnative.dev/docs/animated)
- [JavaScript.info: React Native Animations](https://javascript.info/react-native/animations)

**Example 1:**
```javascript
import React, { useRef, useEffect } from 'react';
import { View, Animated, StyleSheet } from 'react-native';

function FadeInView({ children }) {
  const fadeAnim = useRef(new Animated.Value(0)).current;

  useEffect(() => {
    Animated.timing(fadeAnim, {
      toValue: 1,
      duration: 2000,
      useNativeDriver: true,
    }).start();
  }, [fadeAnim]);

  return (
    <Animated.View style={{ ...styles.fadingContainer, opacity: fadeAnim }}>
      {children}
    </Animated.View>
  );
}

const styles = StyleSheet.create({
  fadingContainer: {
    width: 250,
    height: 50,
    backgroundColor: 'blue',
    justifyContent: 'center',
    alignItems: 'center',
  },
  fadingText: {
    fontSize: 28,
    color: 'white',
  },
});

export default function App() {
  return (
    <View style={styles.container}>
      <FadeInView>
        <Text style={styles.fadingText}>Fading In</Text>
      </FadeInView>
    </View>
  );
}
```

**Example Explanation:**
This

 example demonstrates a simple fade-in animation using the `Animated` API. The `FadeInView` component uses an `Animated.Value` to control the opacity and triggers a timing animation when the component mounts. The child content of `FadeInView` fades in over 2 seconds.

**Example 2:**
```javascript
import React, { useRef, useEffect } from 'react';
import { View, Animated, StyleSheet, Button } from 'react-native';

function SlideInView({ children }) {
  const slideAnim = useRef(new Animated.Value(-100)).current;

  useEffect(() => {
    Animated.spring(slideAnim, {
      toValue: 0,
      useNativeDriver: true,
    }).start();
  }, [slideAnim]);

  return (
    <Animated.View style={{ ...styles.slidingContainer, transform: [{ translateY: slideAnim }] }}>
      {children}
    </Animated.View>
  );
}

const styles = StyleSheet.create({
  slidingContainer: {
    width: 250,
    height: 50,
    backgroundColor: 'green',
    justifyContent: 'center',
    alignItems: 'center',
  },
  slidingText: {
    fontSize: 28,
    color: 'white',
  },
});

export default function App() {
  return (
    <View style={styles.container}>
      <SlideInView>
        <Text style={styles.slidingText}>Sliding In</Text>
      </SlideInView>
    </View>
  );
}
```

**Example Explanation:**
This example demonstrates a simple slide-in animation using the `Animated` API. The `SlideInView` component uses an `Animated.Value` to control the vertical position and triggers a spring animation when the component mounts. The child content of `SlideInView` slides in from the top.

**Resources:**
- [W3Schools: React Native Animations](https://www.w3schools.com/react/react_native_animations.asp)
- [FreeCodeCamp: Animations in React Native](https://www.freecodecamp.org/news/react-native-animations/)

**Expected Output:**
For Example 1:
```
Displays a view with a text that fades in using the Animated API.
```
For Example 2:
```
Displays a view with a text that slides in from the top using the Animated API.
```

---

### Task 9: Implement basic animations with Animated API

**Explanation:**
The `Animated` API in React Native provides tools to create smooth and performant animations. It supports various types of animations, including timing, spring, and decay animations. The `Animated` API allows developers to animate properties such as opacity, position, scale, and rotation, enabling the creation of dynamic and engaging user interfaces.

Implementing basic animations with the `Animated` API is essential for enhancing the user experience in React Native applications. It allows you to add visual feedback, create fluid transitions, and make your app more interactive and appealing.

**Resources:**
- [React Native Official Documentation: Animated](https://reactnative.dev/docs/animated)
- [JavaScript.info: React Native Animations](https://javascript.info/react-native/animations)

**Example 1:**
```javascript
import React, { useRef, useEffect } from 'react';
import { View, Animated, StyleSheet } from 'react-native';

function FadeInView({ children }) {
  const fadeAnim = useRef(new Animated.Value(0)).current;

  useEffect(() => {
    Animated.timing(fadeAnim, {
      toValue: 1,
      duration: 2000,
      useNativeDriver: true,
    }).start();
  }, [fadeAnim]);

  return (
    <Animated.View style={{ ...styles.fadingContainer, opacity: fadeAnim }}>
      {children}
    </Animated.View>
  );
}

const styles = StyleSheet.create({
  fadingContainer: {
    width: 250,
    height: 50,
    backgroundColor: 'blue',
    justifyContent: 'center',
    alignItems: 'center',
  },
  fadingText: {
    fontSize: 28,
    color: 'white',
  },
});

export default function App() {
  return (
    <View style={styles.container}>
      <FadeInView>
        <Text style={styles.fadingText}>Fading In</Text>
      </FadeInView>
    </View>
  );
}
```

**Example Explanation:**
This example demonstrates a simple fade-in animation using the `Animated` API. The `FadeInView` component uses an `Animated.Value` to control the opacity and triggers a timing animation when the component mounts. The child content of `FadeInView` fades in over 2 seconds.

**Example 2:**
```javascript
import React, { useRef, useEffect } from 'react';
import { View, Animated, StyleSheet, Button } from 'react-native';

function SlideInView({ children }) {
  const slideAnim = useRef(new Animated.Value(-100)).current;

  useEffect(() => {
    Animated.spring(slideAnim, {
      toValue: 0,
      useNativeDriver: true,
    }).start();
  }, [slideAnim]);

  return (
    <Animated.View style={{ ...styles.slidingContainer, transform: [{ translateY: slideAnim }] }}>
      {children}
    </Animated.View>
  );
}

const styles = StyleSheet.create({
  slidingContainer: {
    width: 250,
    height: 50,
    backgroundColor: 'green',
    justifyContent: 'center',
    alignItems: 'center',
  },
  slidingText: {
    fontSize: 28,
    color: 'white',
  },
});

export default function App() {
  return (
    <View style={styles.container}>
      <SlideInView>
        <Text style={styles.slidingText}>Sliding In</Text>
      </SlideInView>
    </View>
  );
}
```

**Example Explanation:**
This example demonstrates a simple slide-in animation using the `Animated` API. The `SlideInView` component uses an `Animated.Value` to control the vertical position and triggers a spring animation when the component mounts. The child content of `SlideInView` slides in from the top.

**Resources:**
- [W3Schools: React Native Animations](https://www.w3schools.com/react/react_native_animations.asp)
- [FreeCodeCamp: Animations in React Native](https://www.freecodecamp.org/news/react-native-animations/)

**Expected Output:**
For Example 1:
```
Displays a view with a text that fades in using the Animated API.
```
For Example 2:
```
Displays a view with a text that slides in from the top using the Animated API.
```

---

### Task 10: Learn about PanResponder

**Explanation:**
`PanResponder` in React Native is a powerful API for handling touch gestures, including dragging, swiping, and panning. It allows developers to create custom gesture handling logic by responding to touch events and updating the UI accordingly. `PanResponder` provides detailed information about touch events, such as the gesture's movement, velocity, and direction.

Learning about `PanResponder` is essential for creating complex and interactive gestures in React Native applications. It allows you to build custom touch interactions, implement drag-and-drop functionality, and create responsive and engaging user interfaces.

**Resources:**
- [React Native Official Documentation: PanResponder](https://reactnative.dev/docs/panresponder)
- [JavaScript.info: React Native PanResponder](https://javascript.info/react-native/panresponder)

**Example 1:**
```javascript
import React, { useRef } from 'react';
import { View, Text, StyleSheet, PanResponder, Animated } from 'react-native';

function DraggableBox() {
  const pan = useRef(new Animated.ValueXY()).current;

  const panResponder = useRef(
    PanResponder.create({
      onStartShouldSetPanResponder: () => true,
      onPanResponderMove: Animated.event(
        [
          null,
          { dx: pan.x, dy: pan.y }
        ],
        { useNativeDriver: false }
      ),
      onPanResponderRelease: () => {
        Animated.spring(pan, { toValue: { x: 0, y: 0 }, useNativeDriver: false }).start();
      },
    })
  ).current;

  return (
    <Animated.View
      {...panResponder.panHandlers}
      style={[pan.getLayout(), styles.box]}
    >
      <Text style={styles.text}>Drag Me</Text>
    </Animated.View>
  );
}

const styles = StyleSheet.create({
  box: {
    width: 100,
    height: 100,
    backgroundColor: 'blue',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
  },
  text: {
    color: 'white',
    fontSize: 18,
  },
});

export default DraggableBox;
```

**Example Explanation:**
This example demonstrates using `PanResponder` to create a draggable box. The `PanResponder` handles touch interactions and updates the position of the box using `Animated.ValueXY`. The box returns to its original position when the touch is released.

**Example 2:**
```javascript
import React, { useRef } from 'react';
import { View, Text, StyleSheet, PanResponder, Animated } from 'react-native';

function SwipeableBox() {
  const translateX = useRef(new Animated.Value(0)).current;

  const panResponder = useRef(
    PanResponder.create({
      onStartShouldSetPanResponder: () => true,
      onPanResponderMove: Animated.event(
        [
          null,
          { dx: translateX }
        ],
        { useNativeDriver: false }
      ),
      on

PanResponderRelease: () => {
        Animated.spring(translateX, { toValue: 0, useNativeDriver: false }).start();
      },
    })
  ).current;

  return (
    <Animated.View
      {...panResponder.panHandlers}
      style={[styles.box, { transform: [{ translateX }] }]}
    >
      <Text style={styles.text}>Swipe Me</Text>
    </Animated.View>
  );
}

const styles = StyleSheet.create({
  box: {
    width: 100,
    height: 100,
    backgroundColor: 'red',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
  },
  text: {
    color: 'white',
    fontSize: 18,
  },
});

export default SwipeableBox;
```

**Example Explanation:**
This example demonstrates using `PanResponder` to create a swipeable box. The `PanResponder` handles touch interactions and updates the horizontal position of the box using `Animated.Value`. The box returns to its original position when the touch is released.

**Resources:**
- [W3Schools: React Native PanResponder](https://www.w3schools.com/react/react_native_panresponder.asp)
- [FreeCodeCamp: Using PanResponder in React Native](https://www.freecodecamp.org/news/react-native-panresponder/)

**Expected Output:**
For Example 1:
```
Displays a draggable box that moves with touch interactions using PanResponder.
```
For Example 2:
```
Displays a swipeable box that moves horizontally with touch interactions using PanResponder.
```

---

### Task 11: Use PanResponder for drag-and-drop

**Explanation:**
`PanResponder` in React Native is a versatile API for handling touch gestures, including dragging and dropping. It allows developers to create custom gesture handling logic by responding to touch events and updating the UI accordingly. `PanResponder` provides detailed information about touch events, such as the gesture's movement, velocity, and direction, enabling the creation of complex touch interactions.

Using `PanResponder` for drag-and-drop functionality is essential for creating interactive and engaging user interfaces in React Native applications. It allows you to implement features like draggable items, reorderable lists, and custom touch interactions.

**Resources:**
- [React Native Official Documentation: PanResponder](https://reactnative.dev/docs/panresponder)
- [JavaScript.info: React Native PanResponder](https://javascript.info/react-native/panresponder)

**Example 1:**
```javascript
import React, { useRef } from 'react';
import { View, Text, StyleSheet, PanResponder, Animated } from 'react-native';

function DraggableItem() {
  const pan = useRef(new Animated.ValueXY()).current;

  const panResponder = useRef(
    PanResponder.create({
      onStartShouldSetPanResponder: () => true,
      onPanResponderMove: Animated.event(
        [
          null,
          { dx: pan.x, dy: pan.y }
        ],
        { useNativeDriver: false }
      ),
      onPanResponderRelease: () => {
        Animated.spring(pan, { toValue: { x: 0, y: 0 }, useNativeDriver: false }).start();
      },
    })
  ).current;

  return (
    <Animated.View
      {...panResponder.panHandlers}
      style={[pan.getLayout(), styles.box]}
    >
      <Text style={styles.text}>Drag Me</Text>
    </Animated.View>
  );
}

const styles = StyleSheet.create({
  box: {
    width: 100,
    height: 100,
    backgroundColor: 'blue',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
  },
  text: {
    color: 'white',
    fontSize: 18,
  },
});

export default DraggableItem;
```

**Example Explanation:**
This example demonstrates using `PanResponder` to create a draggable item. The `PanResponder` handles touch interactions and updates the position of the item using `Animated.ValueXY`. The item returns to its original position when the touch is released.

**Example 2:**
```javascript
import React, { useRef } from 'react';
import { View, Text, StyleSheet, PanResponder, Animated } from 'react-native';

function DraggableBox() {
  const pan = useRef(new Animated.ValueXY()).current;

  const panResponder = useRef(
    PanResponder.create({
      onStartShouldSetPanResponder: () => true,
      onPanResponderMove: Animated.event(
        [
          null,
          { dx: pan.x, dy: pan.y }
        ],
        { useNativeDriver: false }
      ),
      onPanResponderRelease: () => {
        Animated.spring(pan, { toValue: { x: 0, y: 0 }, useNativeDriver: false }).start();
      },
    })
  ).current;

  return (
    <Animated.View
      {...panResponder.panHandlers}
      style={[pan.getLayout(), styles.box]}
    >
      <Text style={styles.text}>Drag Me</Text>
    </Animated.View>
  );
}

const styles = StyleSheet.create({
  box: {
    width: 100,
    height: 100,
    backgroundColor: 'red',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
  },
  text: {
    color: 'white',
    fontSize: 18,
  },
});

export default DraggableBox;
```

**Example Explanation:**
This example demonstrates using `PanResponder` to create a draggable box. The `PanResponder` handles touch interactions and updates the position of the box using `Animated.ValueXY`. The box returns to its original position when the touch is released.

**Resources:**
- [W3Schools: React Native PanResponder](https://www.w3schools.com/react/react_native_panresponder.asp)
- [FreeCodeCamp: Using PanResponder for Drag-and-Drop in React Native](https://www.freecodecamp.org/news/react-native-panresponder/)

**Expected Output:**
For Example 1:
```
Displays a draggable item that moves with touch interactions using PanResponder.
```
For Example 2:
```
Displays a draggable box that moves with touch interactions using PanResponder.
```

---

### Task 12: Study networking in React Native

**Explanation:**
Networking in React Native involves making HTTP requests to interact with external APIs and servers. React Native provides built-in APIs like `fetch` and third-party libraries like `axios` to handle network requests. These tools allow developers to send and receive data, handle responses, and manage errors.

Studying networking in React Native is essential for building dynamic and data-driven applications. It enables you to fetch data from remote sources, submit data to servers, and integrate with various web services.

**Resources:**
- [React Native Official Documentation: Networking](https://reactnative.dev/docs/network)
- [JavaScript.info: Networking in React Native](https://javascript.info/react-native/networking)

**Example 1:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet } from 'react-native';

function FetchData() {
  const [data, setData] = useState(null);

  useEffect(() => {
    fetch('https://jsonplaceholder.typicode.com/posts/1')
      .then(response => response.json())
      .then(json => setData(json))
      .catch(error => console.error(error));
  }, []);

  return (
    <View style={styles.container}>
      {data ? (
        <Text style={styles.text}>{data.title}</Text>
      ) : (
        <Text style={styles.text}>Loading...</Text>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
  },
});

export default FetchData;
```

**Example Explanation:**
This example demonstrates using the `fetch` API to make a network request to a public API. The response data is stored in the component state and displayed on the screen. If the data is not yet loaded, a loading message is displayed.

**Example 2:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import axios from 'axios';

function AxiosData() {
  const [data, setData] = useState(null);

  useEffect(() => {
    axios.get('https://jsonplaceholder.typicode.com/posts/1')
      .then(response => setData(response.data))
      .catch(error => console.error(error));
  }, []);

  return (
    <View style={styles.container}>
      {data ? (
        <Text style={styles.text}>{data.title}</Text>
      ) : (
        <Text style={styles.text}>Loading...</Text>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
  },
});

export default AxiosData;
```

**Example Explanation:**
This example demonstrates using the `axios` library to make a network request to a public API. The response data is stored in the component state and displayed on the screen. If the data is not yet loaded, a loading message is displayed.

**Resources:**
- [W3Schools: React Native Networking](https://www.w3schools.com/react/react_native_networking.asp)
- [FreeCodeCamp: Networking in React Native](https://www.free

codecamp.org/news/react-native-networking/)

**Expected Output:**
For Example 1:
```
Displays data fetched from a public API using the fetch API.
```
For Example 2:
```
Displays data fetched from a public API using the axios library.
```

---

### Task 13: Fetch data from an API

**Explanation:**
Fetching data from an API in React Native involves making HTTP requests to retrieve data from external sources. This can be done using the built-in `fetch` API or third-party libraries like `axios`. Fetching data from an API is essential for creating dynamic and data-driven applications, as it allows you to display real-time information, update content, and interact with web services.

Fetching data from an API typically involves sending a request to a specified endpoint, handling the response, and updating the component state with the retrieved data. It is important to handle errors and loading states to ensure a smooth user experience.

**Resources:**
- [React Native Official Documentation: Networking](https://reactnative.dev/docs/network)
- [JavaScript.info: Fetch API](https://javascript.info/fetch-api)

**Example 1:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet } from 'react-native';

function FetchData() {
  const [data, setData] = useState(null);

  useEffect(() => {
    fetch('https://jsonplaceholder.typicode.com/posts/1')
      .then(response => response.json())
      .then(json => setData(json))
      .catch(error => console.error(error));
  }, []);

  return (
    <View style={styles.container}>
      {data ? (
        <Text style={styles.text}>{data.title}</Text>
      ) : (
        <Text style={styles.text}>Loading...</Text>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
  },
});

export default FetchData;
```

**Example Explanation:**
This example demonstrates using the `fetch` API to make a network request to a public API. The response data is stored in the component state and displayed on the screen. If the data is not yet loaded, a loading message is displayed.

**Example 2:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import axios from 'axios';

function AxiosData() {
  const [data, setData] = useState(null);

  useEffect(() => {
    axios.get('https://jsonplaceholder.typicode.com/posts/1')
      .then(response => setData(response.data))
      .catch(error => console.error(error));
  }, []);

  return (
    <View style={styles.container}>
      {data ? (
        <Text style={styles.text}>{data.title}</Text>
      ) : (
        <Text style={styles.text}>Loading...</Text>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
  },
});

export default AxiosData;
```

**Example Explanation:**
This example demonstrates using the `axios` library to make a network request to a public API. The response data is stored in the component state and displayed on the screen. If the data is not yet loaded, a loading message is displayed.

**Resources:**
- [W3Schools: React Native Networking](https://www.w3schools.com/react/react_native_networking.asp)
- [FreeCodeCamp: Fetching Data from an API in React Native](https://www.freecodecamp.org/news/react-native-fetching-data/)

**Expected Output:**
For Example 1:
```
Displays data fetched from a public API using the fetch API.
```
For Example 2:
```
Displays data fetched from a public API using the axios library.
```

---

### Task 14: Handle network requests with Axios

**Explanation:**
Axios is a popular JavaScript library for making HTTP requests. It provides a simple and intuitive API for sending GET, POST, PUT, DELETE, and other types of requests. Axios supports features like request and response interception, automatic transformation of JSON data, and easy error handling. It is commonly used in React Native applications for interacting with RESTful APIs and handling network requests.

Handling network requests with Axios involves importing the library, making HTTP requests to specified endpoints, and processing the responses. Axios also provides methods for handling errors and configuring global defaults, making it a powerful tool for managing network interactions in React Native applications.

**Resources:**
- [Axios GitHub Repository](https://github.com/axios/axios)
- [JavaScript.info: Axios](https://javascript.info/axios)

**Example 1:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import axios from 'axios';

function FetchDataWithAxios() {
  const [data, setData] = useState(null);

  useEffect(() => {
    axios.get('https://jsonplaceholder.typicode.com/posts/1')
      .then(response => setData(response.data))
      .catch(error => console.error(error));
  }, []);

  return (
    <View style={styles.container}>
      {data ? (
        <Text style={styles.text}>{data.title}</Text>
      ) : (
        <Text style={styles.text}>Loading...</Text>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
  },
});

export default FetchDataWithAxios;
```

**Example Explanation:**
This example demonstrates using the `axios` library to make a GET request to a public API. The response data is stored in the component state and displayed on the screen. If the data is not yet loaded, a loading message is displayed.

**Example 2:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet, Button } from 'react-native';
import axios from 'axios';

function PostDataWithAxios() {
  const [response, setResponse] = useState(null);

  const postData = () => {
    axios.post('https://jsonplaceholder.typicode.com/posts', {
      title: 'foo',
      body: 'bar',
      userId: 1,
    })
      .then(response => setResponse(response.data))
      .catch(error => console.error(error));
  };

  return (
    <View style={styles.container}>
      <Button title="Post Data" onPress={postData} />
      {response && (
        <Text style={styles.text}>Response: {JSON.stringify(response)}</Text>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
    marginTop: 20,
  },
});

export default PostDataWithAxios;
```

**Example Explanation:**
This example demonstrates using the `axios` library to make a POST request to a public API. The `postData` function sends a POST request with some sample data, and the response is stored in the component state and displayed on the screen.

**Resources:**
- [W3Schools: React Native Axios](https://www.w3schools.com/react/react_native_axios.asp)
- [FreeCodeCamp: Handling Network Requests with Axios in React Native](https://www.freecodecamp.org/news/react-native-axios/)

**Expected Output:**
For Example 1:
```
Displays data fetched from a public API using the axios library.
```
For Example 2:
```
Displays the response of a POST request made to a public API using the axios library.
```

---

### Task 15: Learn about offline support

**Explanation:**
Offline support in React Native applications allows the app to function correctly even when there is no internet connection. This involves caching data locally, detecting network status changes, and providing fallback content or features when offline. Libraries like `redux-persist` and `AsyncStorage` are commonly used to implement offline support.

Learning about offline support is essential for creating resilient and user-friendly applications. It ensures that users can continue to interact with the app and access previously fetched data even when they lose connectivity. Offline support improves the overall user experience and reliability of the application.

**Resources:**
- [React Native Official Documentation: Offline Support](https://reactnative.dev/docs/offline-support)
- [JavaScript.info: Offline Support in React Native](https://javascript.info/react-native/offline-support)

**Example 1:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet, Button, AsyncStorage } from 'react-native';

function OfflineSupport() {
  const [data, setData] = useState(null);

  const fetchData = async () => {
    try {
      const response = await fetch('https://jsonplaceholder.typicode.com/posts/1');
      const json = await response.json();
      await AsyncStorage.setItem('offlineData', JSON.stringify(json));
      setData(json);
    } catch (error) {
      const offlineData = await AsyncStorage.getItem('offlineData');
      if (offlineData) {
        setData(JSON.parse(offlineData));
      }
    }


  };

  useEffect(() => {
    fetchData();
  }, []);

  return (
    <View style={styles.container}>
      {data ? (
        <Text style={styles.text}>{data.title}</Text>
      ) : (
        <Text style={styles.text}>Loading...</Text>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
  },
});

export default OfflineSupport;
```

**Example Explanation:**
This example demonstrates offline support using `AsyncStorage`. The `fetchData` function attempts to fetch data from an API and stores it in `AsyncStorage`. If the network request fails, it retrieves the data from `AsyncStorage` and displays it on the screen.

**Example 2:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet, Button, AsyncStorage } from 'react-native';
import NetInfo from '@react-native-community/netinfo';

function OfflineStatus() {
  const [isConnected, setIsConnected] = useState(true);

  useEffect(() => {
    const unsubscribe = NetInfo.addEventListener(state => {
      setIsConnected(state.isConnected);
    });

    return () => unsubscribe();
  }, []);

  return (
    <View style={styles.container}>
      <Text style={styles.text}>
        {isConnected ? 'Online' : 'Offline'}
      </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
  },
});

export default OfflineStatus;
```

**Example Explanation:**
This example demonstrates using the `NetInfo` library to detect network status changes. The `OfflineStatus` component displays whether the device is currently online or offline based on the network status.

**Resources:**
- [W3Schools: React Native Offline Support](https://www.w3schools.com/react/react_native_offline.asp)
- [FreeCodeCamp: Implementing Offline Support in React Native](https://www.freecodecamp.org/news/react-native-offline-support/)

**Expected Output:**
For Example 1:
```
Displays data fetched from an API and cached in AsyncStorage for offline support.
```
For Example 2:
```
Displays the current network status (online or offline) using the NetInfo library.
```

---

### Task 16: Implement offline caching

**Explanation:**
Offline caching in React Native involves storing data locally to ensure the application can function without an internet connection. This can be achieved using tools like `AsyncStorage`, `redux-persist`, or `react-native-mmkv`. Offline caching allows the app to access previously fetched data, maintain user interactions, and provide a seamless experience even when offline.

Implementing offline caching is essential for creating resilient and user-friendly applications. It ensures that users can continue to interact with the app and access important data even when they lose connectivity, improving the overall user experience and reliability of the application.

**Resources:**
- [React Native Official Documentation: Offline Support](https://reactnative.dev/docs/offline-support)
- [JavaScript.info: Offline Caching in React Native](https://javascript.info/react-native/offline-caching)

**Example 1:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet, AsyncStorage } from 'react-native';

function OfflineCaching() {
  const [data, setData] = useState(null);

  const fetchData = async () => {
    try {
      const response = await fetch('https://jsonplaceholder.typicode.com/posts/1');
      const json = await response.json();
      await AsyncStorage.setItem('offlineData', JSON.stringify(json));
      setData(json);
    } catch (error) {
      const offlineData = await AsyncStorage.getItem('offlineData');
      if (offlineData) {
        setData(JSON.parse(offlineData));
      }
    }
  };

  useEffect(() => {
    fetchData();
  }, []);

  return (
    <View style={styles.container}>
      {data ? (
        <Text style={styles.text}>{data.title}</Text>
      ) : (
        <Text style={styles.text}>Loading...</Text>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
  },
});

export default OfflineCaching;
```

**Example Explanation:**
This example demonstrates offline caching using `AsyncStorage`. The `fetchData` function attempts to fetch data from an API and stores it in `AsyncStorage`. If the network request fails, it retrieves the data from `AsyncStorage` and displays it on the screen.

**Example 2:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet, AsyncStorage, Button } from 'react-native';

function ManualCacheRefresh() {
  const [data, setData] = useState(null);

  const fetchData = async () => {
    try {
      const response = await fetch('https://jsonplaceholder.typicode.com/posts/1');
      const json = await response.json();
      await AsyncStorage.setItem('offlineData', JSON.stringify(json));
      setData(json);
    } catch (error) {
      const offlineData = await AsyncStorage.getItem('offlineData');
      if (offlineData) {
        setData(JSON.parse(offlineData));
      }
    }
  };

  useEffect(() => {
    fetchData();
  }, []);

  return (
    <View style={styles.container}>
      {data ? (
        <Text style={styles.text}>{data.title}</Text>
      ) : (
        <Text style={styles.text}>Loading...</Text>
      )}
      <Button title="Refresh Data" onPress={fetchData} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
  },
});

export default ManualCacheRefresh;
```

**Example Explanation:**
This example demonstrates offline caching with manual data refresh using `AsyncStorage`. The `fetchData` function attempts to fetch data from an API and stores it in `AsyncStorage`. If the network request fails, it retrieves the data from `AsyncStorage` and displays it on the screen. A button is provided to manually refresh the data.

**Resources:**
- [W3Schools: React Native Offline Caching](https://www.w3schools.com/react/react_native_offline.asp)
- [FreeCodeCamp: Implementing Offline Caching in React Native](https://www.freecodecamp.org/news/react-native-offline-caching/)

**Expected Output:**
For Example 1:
```
Displays data fetched from an API and cached in AsyncStorage for offline support.
```
For Example 2:
```
Displays data fetched from an API and cached in AsyncStorage with a manual refresh button for offline support.
```

---

### Task 17: Study image handling

**Explanation:**
Image handling in React Native involves displaying, loading, and managing images within the application. The `Image` component is used to display images from various sources, including local files, network URLs, and base64 encoded strings. Understanding image handling is essential for creating visually appealing and efficient applications that load images quickly and handle different image formats and sizes effectively.

Studying image handling is crucial for optimizing the performance and user experience of your React Native application. It allows you to manage image loading, caching, and rendering efficiently, ensuring that images are displayed correctly and perform well across different devices.

**Resources:**
- [React Native Official Documentation: Image](https://reactnative.dev/docs/image)
- [JavaScript.info: Image Handling in React Native](https://javascript.info/react-native/image-handling)

**Example 1:**
```javascript
import React from 'react';
import { View, Image, StyleSheet } from 'react-native';

function DisplayImage() {
  return (
    <View style={styles.container}>
      <Image
        style={styles.image}
        source={{ uri: 'https://example.com/image.jpg' }}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  image: {
    width: 200,
    height: 200,
    borderRadius: 10,
  },
});

export default DisplayImage;
```

**Example Explanation:**
This example demonstrates displaying an image from a network URL using the `Image` component. The `source` prop is used to specify the image URL, and the image is styled with specific width, height, and border radius.

**Example 2:**
```javascript
import React from 'react';
import { View, Image, StyleSheet } from 'react-native';

function LocalImage() {
  return (
    <View style={styles.container}>
      <Image
        style={styles.image}
        source={require('./assets/local-image.jpg')}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  image: {
    width: 200,
    height: 200,
    borderRadius: 10,
  },
});

export default LocalImage;
```



**Example Explanation:**
This example demonstrates displaying a local image using the `Image` component. The `source` prop is used to specify the path to the local image file, and the image is styled with specific width, height, and border radius.

**Resources:**
- [W3Schools: React Native Image Handling](https://www.w3schools.com/react/react_native_image.asp)
- [FreeCodeCamp: Handling Images in React Native](https://www.freecodecamp.org/news/react-native-image-handling/)

**Expected Output:**
For Example 1:
```
Displays an image from a network URL using the Image component.
```
For Example 2:
```
Displays a local image using the Image component.
```

---

### Task 18: Use Image component effectively

**Explanation:**
The `Image` component in React Native is used to display images from various sources, including local files, network URLs, and base64 encoded strings. The `Image` component supports properties like `resizeMode`, `onLoad`, `onError`, and `onProgress` to control the appearance and behavior of images. Using the `Image` component effectively involves understanding these properties and optimizing image loading and rendering for better performance.

Using the `Image` component effectively is essential for creating visually appealing and efficient applications. It allows you to manage image loading, handle errors, and optimize image rendering to ensure a smooth user experience.

**Resources:**
- [React Native Official Documentation: Image](https://reactnative.dev/docs/image)
- [JavaScript.info: Using Image Component in React Native](https://javascript.info/react-native/image-component)

**Example 1:**
```javascript
import React from 'react';
import { View, Image, StyleSheet } from 'react-native';

function NetworkImage() {
  return (
    <View style={styles.container}>
      <Image
        style={styles.image}
        source={{ uri: 'https://example.com/image.jpg' }}
        resizeMode="cover"
        onLoad={() => console.log('Image loaded successfully')}
        onError={() => console.error('Failed to load image')}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  image: {
    width: 200,
    height: 200,
    borderRadius: 10,
  },
});

export default NetworkImage;
```

**Example Explanation:**
This example demonstrates using the `Image` component to display a network image with `resizeMode`, `onLoad`, and `onError` properties. The `resizeMode` property is set to `cover`, and console messages are logged when the image loads successfully or fails to load.

**Example 2:**
```javascript
import React from 'react';
import { View, Image, StyleSheet } from 'react-native';

function LocalImageComponent() {
  return (
    <View style={styles.container}>
      <Image
        style={styles.image}
        source={require('./assets/local-image.jpg')}
        resizeMode="contain"
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  image: {
    width: 200,
    height: 200,
    borderRadius: 10,
  },
});

export default LocalImageComponent;
```

**Example Explanation:**
This example demonstrates using the `Image` component to display a local image with the `resizeMode` property set to `contain`. The image is styled with specific width, height, and border radius.

**Resources:**
- [W3Schools: React Native Image Component](https://www.w3schools.com/react/react_native_image.asp)
- [FreeCodeCamp: Using Image Component in React Native](https://www.freecodecamp.org/news/react-native-image-component/)

**Expected Output:**
For Example 1:
```
Displays a network image with cover resize mode and logs messages when the image loads or fails to load.
```
For Example 2:
```
Displays a local image with contain resize mode using the Image component.
```

---

### Task 19: Learn about vector icons

**Explanation:**
Vector icons are scalable icons that can be used in React Native applications to enhance the user interface. Libraries like `react-native-vector-icons` provide a wide range of vector icons that can be easily integrated into the application. Vector icons are resolution-independent, meaning they look sharp on all screen sizes and densities, making them ideal for use in mobile applications.

Learning about vector icons is essential for creating visually appealing and user-friendly interfaces. It allows you to use high-quality icons in your application, customize their appearance, and ensure they look great on all devices.

**Resources:**
- [React Native Vector Icons Documentation](https://github.com/oblador/react-native-vector-icons)
- [JavaScript.info: Vector Icons in React Native](https://javascript.info/react-native/vector-icons)

**Example 1:**
```javascript
import React from 'react';
import { View, StyleSheet } from 'react-native';
import Icon from 'react-native-vector-icons/Ionicons';

function IconExample() {
  return (
    <View style={styles.container}>
      <Icon name="ios-star" size={50} color="gold" />
      <Icon name="ios-heart" size={50} color="red" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default IconExample;
```

**Example Explanation:**
This example demonstrates using `react-native-vector-icons` to display vector icons. The `Icon` component from `react-native-vector-icons/Ionicons` is used to display star and heart icons with different colors and sizes.

**Example 2:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import Icon from 'react-native-vector-icons/FontAwesome';

function IconWithText() {
  return (
    <View style={styles.container}>
      <Icon name="rocket" size={50} color="blue" />
      <Text style={styles.text}>Launch</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 20,
    marginTop: 10,
  },
});

export default IconWithText;
```

**Example Explanation:**
This example demonstrates using `react-native-vector-icons` to display a vector icon with accompanying text. The `Icon` component from `react-native-vector-icons/FontAwesome` is used to display a rocket icon, and a `Text` component displays the word "Launch."

**Resources:**
- [W3Schools: React Native Vector Icons](https://www.w3schools.com/react/react_native_vector_icons.asp)
- [FreeCodeCamp: Using Vector Icons in React Native](https://www.freecodecamp.org/news/react-native-vector-icons/)

**Expected Output:**
For Example 1:
```
Displays vector icons (star and heart) with different colors and sizes using react-native-vector-icons.
```
For Example 2:
```
Displays a vector icon (rocket) with accompanying text using react-native-vector-icons.
```

---

### Task 20: Use vector icons in the app

**Explanation:**
Using vector icons in a React Native application involves integrating a vector icon library, such as `react-native-vector-icons`, and using the provided icons to enhance the user interface. Vector icons are scalable and resolution-independent, making them ideal for mobile applications. They can be customized with different colors, sizes, and styles to match the application's design.

Using vector icons in the app is essential for creating visually appealing and user-friendly interfaces. It allows you to use high-quality icons that look sharp on all screen sizes and densities, ensuring a consistent and polished appearance across the application.

**Resources:**
- [React Native Vector Icons Documentation](https://github.com/oblador/react-native-vector-icons)
- [JavaScript.info: Using Vector Icons in React Native](https://javascript.info/react-native/vector-icons)

**Example 1:**
```javascript
import React from 'react';
import { View, StyleSheet } from 'react-native';
import Icon from 'react-native-vector-icons/Ionicons';

function AppIcons() {
  return (
    <View style={styles.container}>
      <Icon name="ios-home" size={30} color="black" />
      <Icon name="ios-search" size={30} color="black" />
      <Icon name="ios-notifications" size={30} color="black" />
      <Icon name="ios-settings" size={30} color="black" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    padding: 20,
  },
});

export default AppIcons;
```

**Example Explanation:**
This example demonstrates using `react-native-vector-icons` to display a set of commonly used icons (home, search, notifications, and settings) in a row. The `Icon` component from `react-native-vector-icons/Ionicons` is used to display the icons with the same size and color.

**Example 2:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import Icon from 'react-native-vector-icons/FontAwesome';

function IconWithLabel({ name, label }) {
  return (
    <View style={styles.iconContainer}>
      <Icon name={name} size={40} color="blue" />


      <Text style={styles.label}>{label}</Text>
    </View>
  );
}

function IconList() {
  return (
    <View style={styles.container}>
      <IconWithLabel name="home" label="Home" />
      <IconWithLabel name="user" label="Profile" />
      <IconWithLabel name="bell" label="Notifications" />
      <IconWithLabel name="cog" label="Settings" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    padding: 20,
  },
  iconContainer: {
    alignItems: 'center',
  },
  label: {
    marginTop: 5,
    fontSize: 14,
  },
});

export default IconList;
```

**Example Explanation:**
This example demonstrates creating a custom component `IconWithLabel` that displays a vector icon with a label. The `IconList` component uses this custom component to display a list of icons with corresponding labels in a row.

**Resources:**
- [W3Schools: React Native Vector Icons](https://www.w3schools.com/react/react_native_vector_icons.asp)
- [FreeCodeCamp: Using Vector Icons in React Native](https://www.freecodecamp.org/news/react-native-vector-icons/)

**Expected Output:**
For Example 1:
```
Displays a row of commonly used icons (home, search, notifications, and settings) using react-native-vector-icons.
```
For Example 2:
```
Displays a row of icons with corresponding labels using a custom component and react-native-vector-icons.
```
---
