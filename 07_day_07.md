for day 7, tasks listed between
"""
Set up React Native development environment.
Install React Native CLI or use Expo.
Create a new React Native project.
Understand project structure.
Study basic components (View, Text, Image).
Create a simple screen layout.
Learn about styling in React Native.
Use StyleSheet for component styles.
Implement Flexbox for layout.
Study text input and buttons.
Create forms with inputs and buttons.
Study ScrollView and FlatList.
Implement scrollable content.
Learn about navigation with React Navigation.
Set up basic navigation.
Create a stack navigator.
Pass parameters between screens.
Study state management in React Native.
Use `useState` and `useEffect` hooks.
Build a simple multi-screen app.
""", for each task give me 2 paragraphs explaining the topic, 2 online resources to refer, 2 examples to practice, 2 paragraph explaining each example, 2 online resources to refer, also give me the code that i should use to practice and give the expected output

---
### Task 1: Set up React Native development environment

**Explanation:**
Setting up the React Native development environment involves installing the necessary tools and dependencies to develop and run React Native applications. This includes Node.js, npm (or Yarn), React Native CLI, and Android Studio (for Android) or Xcode (for iOS). You can also use Expo, an open-source platform for React Native, which simplifies the setup process and allows you to develop and run apps on your device or emulator without complex configurations.

Setting up the environment correctly ensures that you have all the necessary tools to develop, debug, and test your React Native applications. It also helps avoid issues related to missing dependencies or incorrect configurations.

**Resources:**
- [React Native Official Documentation: Environment Setup](https://reactnative.dev/docs/environment-setup)
- [Expo Documentation: Getting Started](https://docs.expo.dev/get-started/installation/)

**Example 1:**
```sh
# Installing Node.js and npm
brew install node

# Installing React Native CLI
npm install -g react-native-cli
```

**Example Explanation:**
This example shows how to install Node.js and npm using Homebrew and then install the React Native CLI globally using npm.

**Example 2:**
```sh
# Installing Expo CLI
npm install -g expo-cli

# Creating a new Expo project
expo init my-new-project
```

**Example Explanation:**
This example shows how to install the Expo CLI globally using npm and then create a new Expo project using the `expo init` command.

**Resources:**
- [W3Schools: React Native Environment Setup](https://www.w3schools.com/react/react_native_environment.asp)
- [FreeCodeCamp: How to Set Up a React Native Development Environment](https://www.freecodecamp.org/news/setup-react-native-development-environment-android/)

**Expected Output:**
For Example 1:
```
Node.js and npm are installed, and React Native CLI is set up on your system.
```
For Example 2:
```
Expo CLI is installed, and a new Expo project is created.
```

---

### Task 2: Install React Native CLI or use Expo

**Explanation:**
Installing the React Native CLI or using Expo are two approaches to starting React Native development. The React Native CLI provides more control and flexibility for native development, allowing you to integrate native modules and customize build configurations. Expo, on the other hand, simplifies the setup process and provides a managed workflow with pre-configured native modules and easy integration with Expo services.

Choosing between React Native CLI and Expo depends on your project requirements and development preferences. If you need to work with custom native modules or require advanced build configurations, the React Native CLI is a better choice. For quick setup and ease of use, Expo is ideal.

**Resources:**
- [React Native Official Documentation: Getting Started](https://reactnative.dev/docs/getting-started)
- [Expo Documentation: Overview](https://docs.expo.dev/)

**Example 1:**
```sh
# Installing React Native CLI
npm install -g react-native-cli

# Creating a new React Native project
react-native init MyNewProject
```

**Example Explanation:**
This example shows how to install the React Native CLI globally using npm and then create a new React Native project using the `react-native init` command.

**Example 2:**
```sh
# Installing Expo CLI
npm install -g expo-cli

# Creating a new Expo project
expo init my-new-project
```

**Example Explanation:**
This example shows how to install the Expo CLI globally using npm and then create a new Expo project using the `expo init` command.

**Resources:**
- [W3Schools: Install React Native CLI](https://www.w3schools.com/react/react_native_environment.asp)
- [FreeCodeCamp: How to Install React Native CLI and Expo](https://www.freecodecamp.org/news/install-react-native-cli-expo/)

**Expected Output:**
For Example 1:
```
React Native CLI is installed, and a new React Native project is created.
```
For Example 2:
```
Expo CLI is installed, and a new Expo project is created.
```

---

### Task 3: Create a new React Native project

**Explanation:**
Creating a new React Native project involves setting up the initial project structure and installing necessary dependencies. Whether you use React Native CLI or Expo, the project setup includes creating the base folder structure, configuring initial settings, and installing core dependencies required for React Native development.

Creating a new project provides a starting point for developing your React Native application. It sets up the essential files and configurations needed to build, run, and test your app on Android and iOS devices.

**Resources:**
- [React Native Official Documentation: Getting Started](https://reactnative.dev/docs/getting-started)
- [Expo Documentation: First Steps](https://docs.expo.dev/get-started/installation/)

**Example 1:**
```sh
# Creating a new React Native project using React Native CLI
react-native init MyNewProject
cd MyNewProject
react-native run-android # or react-native run-ios
```

**Example Explanation:**
This example shows how to create a new React Native project using React Native CLI, navigate to the project directory, and run the project on an Android or iOS device.

**Example 2:**
```sh
# Creating a new Expo project
expo init my-new-project
cd my-new-project
expo start
```

**Example Explanation:**
This example shows how to create a new Expo project, navigate to the project directory, and start the Expo development server.

**Resources:**
- [W3Schools: Create React Native Project](https://www.w3schools.com/react/react_native_getting_started.asp)
- [FreeCodeCamp: Creating a New React Native Project](https://www.freecodecamp.org/news/create-react-native-project/)

**Expected Output:**
For Example 1:
```
A new React Native project is created, and the app runs on an Android or iOS device.
```
For Example 2:
```
A new Expo project is created, and the Expo development server starts.
```

---

### Task 4: Understand project structure

**Explanation:**
Understanding the project structure of a React Native application is crucial for navigating and organizing your code. The project structure typically includes directories for source code, assets, configuration files, and platform-specific code (for Android and iOS). Key directories include `src` for source code, `assets` for images and fonts, and `android` and `ios` for native code.

Understanding the project structure helps you maintain a clean and organized codebase. It allows you to quickly locate files, manage dependencies, and configure settings for different platforms. A well-organized project structure also makes it easier for team collaboration and code maintenance.

**Resources:**
- [React Native Official Documentation: Project Structure](https://reactnative.dev/docs/structure)
- [JavaScript.info: React Native Project Structure](https://javascript.info/react-native/project-structure)

**Example 1:**
```
MyNewProject/
├── android/
├── ios/
├── node_modules/
├── src/
│   ├── components/
│   ├── screens/
│   ├── App.js
├── assets/
│   ├── images/
│   ├── fonts/
├── package.json
├── App.js
```

**Example Explanation:**
This example shows a typical project structure for a React Native application, including directories for Android and iOS native code, source code, assets, and dependencies.

**Example 2:**
```sh
# Exploring the project structure
cd MyNewProject
tree .
```

**Example Explanation:**
This example shows how to explore the project structure using the `tree` command, which displays the directory structure of the project.

**Resources:**
- [W3Schools: React Native Project Structure](https://www.w3schools.com/react/react_native_project_structure.asp)
- [FreeCodeCamp: Understanding React Native Project Structure](https://www.freecodecamp.org/news/react-native-project-structure/)

**Expected Output:**
For Example 1:
```
Displays the typical project structure of a React Native application.
```
For Example 2:
```
Displays the directory structure of the project using the tree command.
```

---

### Task 5: Study basic components (View, Text, Image)

**Explanation:**
Basic components in React Native include `View`, `Text`, and `Image`, which are the building blocks of any React Native application. `View` is a container component used for layout and grouping other components. `Text` is used for displaying text, and `Image` is used for displaying images. These components are similar to their HTML counterparts but are optimized for mobile development.

Studying basic components is essential for understanding how to build user interfaces in React Native. These components provide the foundation for creating layouts, displaying content, and interacting with users. Mastering these components allows you to build complex and responsive mobile applications.

**Resources:**
- [React Native Official Documentation: Components](https://reactnative.dev/docs/components-and-apis)
- [JavaScript.info: React Native Components](https://javascript.info/react-native/components)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, Image, StyleSheet } from 'react-native';

function BasicComponents() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Hello, React Native!</Text>
      <Image
        style={styles.image}
        source={{ uri: 'https://reactnative.dev/img/tiny_logo.png' }}
      />
    </View>


  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 20,
    fontWeight: 'bold',
  },
  image: {
    width: 100,
    height: 100,
  },
});

export default BasicComponents;
```

**Example Explanation:**
This example demonstrates the use of basic components (`View`, `Text`, and `Image`) in a React Native application. The `View` component is used as a container, the `Text` component displays a message, and the `Image` component displays an image from a URL.

**Example 2:**
```javascript
import React from 'react';
import { View, Text, Image, StyleSheet } from 'react-native';

function WelcomeScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Welcome to My App</Text>
      <Image
        style={styles.logo}
        source={require('./assets/logo.png')}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
  },
  logo: {
    width: 150,
    height: 150,
  },
});

export default WelcomeScreen;
```

**Example Explanation:**
This example demonstrates a welcome screen using basic components (`View`, `Text`, and `Image`). The `View` component is used as a container, the `Text` component displays a title, and the `Image` component displays a local image.

**Resources:**
- [W3Schools: React Native View, Text, and Image](https://www.w3schools.com/react/react_native_components.asp)
- [FreeCodeCamp: Basic Components in React Native](https://www.freecodecamp.org/news/react-native-basic-components/)

**Expected Output:**
For Example 1:
```
Displays a message and an image using basic React Native components.
```
For Example 2:
```
Displays a welcome screen with a title and logo using basic React Native components.
```

---

### Task 6: Create a simple screen layout

**Explanation:**
Creating a simple screen layout in React Native involves using basic components like `View`, `Text`, and `Image` to structure the content and layout of the screen. React Native uses Flexbox for layout, which allows you to create responsive and flexible designs. The `StyleSheet` API is used to define styles for the components.

Creating a simple screen layout is essential for understanding how to build user interfaces in React Native. It helps you practice arranging components, applying styles, and creating responsive layouts that adapt to different screen sizes and orientations.

**Resources:**
- [React Native Official Documentation: Layout with Flexbox](https://reactnative.dev/docs/flexbox)
- [JavaScript.info: React Native Layout](https://javascript.info/react-native/layout)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function SimpleLayout() {
  return (
    <View style={styles.container}>
      <View style={styles.header}>
        <Text style={styles.headerText}>Header</Text>
      </View>
      <View style={styles.content}>
        <Text style={styles.contentText}>Content goes here.</Text>
      </View>
      <View style={styles.footer}>
        <Text style={styles.footerText}>Footer</Text>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f8f8f8',
  },
  content: {
    flex: 3,
    justifyContent: 'center',
    alignItems: 'center',
  },
  footer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f8f8f8',
  },
  headerText: {
    fontSize: 24,
    fontWeight: 'bold',
  },
  contentText: {
    fontSize: 18,
  },
  footerText: {
    fontSize: 18,
    fontStyle: 'italic',
  },
});

export default SimpleLayout;
```

**Example Explanation:**
This example demonstrates creating a simple screen layout with a header, content area, and footer. The `View` components are used to create sections, and the `Text` components display content. The layout uses Flexbox for responsive design.

**Example 2:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function WelcomeLayout() {
  return (
    <View style={styles.container}>
      <View style={styles.header}>
        <Text style={styles.title}>Welcome to My App</Text>
      </View>
      <View style={styles.content}>
        <Text style={styles.subtitle}>Get started by exploring the app.</Text>
      </View>
      <View style={styles.footer}>
        <Text style={styles.footerText}>Footer Content</Text>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  header: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#e0e0e0',
  },
  content: {
    flex: 2,
    justifyContent: 'center',
    alignItems: 'center',
  },
  footer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#e0e0e0',
  },
  title: {
    fontSize: 28,
    fontWeight: 'bold',
  },
  subtitle: {
    fontSize: 20,
  },
  footerText: {
    fontSize: 16,
    fontStyle: 'italic',
  },
});

export default WelcomeLayout;
```

**Example Explanation:**
This example demonstrates creating a welcome screen layout with a header, content area, and footer. The `View` components create sections, and the `Text` components display content. The layout uses Flexbox for a responsive design and padding for spacing.

**Resources:**
- [W3Schools: React Native Layout](https://www.w3schools.com/react/react_native_layout.asp)
- [FreeCodeCamp: Creating a Simple Screen Layout in React Native](https://www.freecodecamp.org/news/react-native-simple-layout/)

**Expected Output:**
For Example 1:
```
Displays a screen layout with a header, content area, and footer using basic components and Flexbox for responsive design.
```
For Example 2:
```
Displays a welcome screen layout with a header, content area, and footer using basic components, Flexbox, and padding for spacing.
```

---

### Task 7: Learn about styling in React Native

**Explanation:**
Styling in React Native is similar to styling in CSS but uses the `StyleSheet` API to define styles for components. React Native supports various style properties, including layout, color, typography, and spacing. Styles can be applied inline, using the `StyleSheet` API, or with external style files.

Learning about styling in React Native is essential for creating visually appealing and consistent user interfaces. It allows you to control the appearance of your components, apply responsive designs, and ensure a cohesive look and feel across your application.

**Resources:**
- [React Native Official Documentation: Style](https://reactnative.dev/docs/style)
- [JavaScript.info: React Native Styling](https://javascript.info/react-native/styling)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function StyledComponent() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Styled Component</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f0f0f0',
  },
  text: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#333',
  },
});

export default StyledComponent;
```

**Example Explanation:**
This example demonstrates defining and applying styles using the `StyleSheet` API. The `container` style is applied to the `View` component, and the `text` style is applied to the `Text` component.

**Example 2:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function CustomButton() {
  return (
    <View style={styles.button}>
      <Text style={styles.buttonText}>Click Me</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  button: {
    backgroundColor: '#6200ee',
    padding: 10,
    borderRadius: 5,
    alignItems: 'center',
  },
  buttonText: {
    color: '#fff',
    fontSize: 18,
  },
});

export default CustomButton;
```

**Example Explanation:**
This example demonstrates creating a custom button with styles defined using the `StyleSheet` API. The `button` style is applied to the `View` component, and the `buttonText

` style is applied to the `Text` component.

**Resources:**
- [W3Schools: React Native Style](https://www.w3schools.com/react/react_native_style.asp)
- [FreeCodeCamp: Styling in React Native](https://www.freecodecamp.org/news/react-native-styling/)

**Expected Output:**
For Example 1:
```
Displays a styled component with a container and text using the StyleSheet API.
```
For Example 2:
```
Displays a custom button with styles for the button and text using the StyleSheet API.
```

---

### Task 8: Use StyleSheet for component styles

**Explanation:**
The `StyleSheet` API in React Native is used to define and apply styles to components. It provides a way to create style objects that can be referenced by components to apply specific styles. The `StyleSheet` API supports various style properties, including layout, color, typography, and spacing.

Using `StyleSheet` for component styles allows you to organize your styles in a structured and maintainable way. It helps you create reusable style objects, apply consistent styles across components, and optimize performance by avoiding inline styles.

**Resources:**
- [React Native Official Documentation: StyleSheet](https://reactnative.dev/docs/stylesheet)
- [JavaScript.info: React Native StyleSheet](https://javascript.info/react-native/stylesheet)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function ProfileCard() {
  return (
    <View style={styles.card}>
      <Text style={styles.name}>John Doe</Text>
      <Text style={styles.email}>john.doe@example.com</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  card: {
    padding: 16,
    backgroundColor: '#fff',
    borderRadius: 8,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 8,
    elevation: 2,
  },
  name: {
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 8,
  },
  email: {
    fontSize: 16,
    color: '#666',
  },
});

export default ProfileCard;
```

**Example Explanation:**
This example demonstrates using the `StyleSheet` API to define styles for a profile card component. The `card` style is applied to the `View` component, and the `name` and `email` styles are applied to the `Text` components.

**Example 2:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function NotificationBadge() {
  return (
    <View style={styles.badge}>
      <Text style={styles.badgeText}>5</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  badge: {
    backgroundColor: '#ff0000',
    padding: 8,
    borderRadius: 50,
    width: 24,
    height: 24,
    justifyContent: 'center',
    alignItems: 'center',
  },
  badgeText: {
    color: '#fff',
    fontSize: 14,
    fontWeight: 'bold',
  },
});

export default NotificationBadge;
```

**Example Explanation:**
This example demonstrates using the `StyleSheet` API to define styles for a notification badge component. The `badge` style is applied to the `View` component, and the `badgeText` style is applied to the `Text` component.

**Resources:**
- [W3Schools: React Native StyleSheet](https://www.w3schools.com/react/react_native_stylesheet.asp)
- [FreeCodeCamp: Using StyleSheet in React Native](https://www.freecodecamp.org/news/react-native-stylesheet/)

**Expected Output:**
For Example 1:
```
Displays a profile card with styles for the card, name, and email using the StyleSheet API.
```
For Example 2:
```
Displays a notification badge with styles for the badge and text using the StyleSheet API.
```

---

### Task 9: Implement Flexbox for layout

**Explanation:**
Flexbox is a layout model that allows you to create flexible and responsive layouts in React Native. Flexbox provides a set of properties, such as `flexDirection`, `justifyContent`, `alignItems`, and `flex`, to control the arrangement and alignment of components within a container. Flexbox makes it easy to create complex layouts that adapt to different screen sizes and orientations.

Implementing Flexbox for layout is essential for building responsive and flexible user interfaces in React Native. It allows you to control the positioning and sizing of components, create multi-column layouts, and align items within a container.

**Resources:**
- [React Native Official Documentation: Layout with Flexbox](https://reactnative.dev/docs/flexbox)
- [JavaScript.info: React Native Flexbox](https://javascript.info/react-native/flexbox)

**Example 1:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function FlexboxLayout() {
  return (
    <View style={styles.container}>
      <View style={styles.box}>
        <Text>Box 1</Text>
      </View>
      <View style={styles.box}>
        <Text>Box 2</Text>
      </View>
      <View style={styles.box}>
        <Text>Box 3</Text>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-around',
    alignItems: 'center',
  },
  box: {
    width: 80,
    height: 80,
    backgroundColor: '#ccc',
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default FlexboxLayout;
```

**Example Explanation:**
This example demonstrates using Flexbox to create a layout with three boxes arranged in a row. The `container` style uses `flexDirection: 'row'` to arrange the boxes horizontally and `justifyContent: 'space-around'` to distribute space evenly around the boxes. The `box` style defines the size and alignment of each box.

**Example 2:**
```javascript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

function FlexboxColumnLayout() {
  return (
    <View style={styles.container}>
      <View style={styles.box}>
        <Text>Box 1</Text>
      </View>
      <View style={styles.box}>
        <Text>Box 2</Text>
      </View>
      <View style={styles.box}>
        <Text>Box 3</Text>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
  },
  box: {
    width: 100,
    height: 100,
    backgroundColor: '#bbb',
    justifyContent: 'center',
    alignItems: 'center',
    margin: 10,
  },
});

export default FlexboxColumnLayout;
```

**Example Explanation:**
This example demonstrates using Flexbox to create a layout with three boxes arranged in a column. The `container` style uses `flexDirection: 'column'` to arrange the boxes vertically and `justifyContent: 'center'` to center the boxes within the container. The `box` style defines the size, alignment, and margin of each box.

**Resources:**
- [W3Schools: React Native Flexbox](https://www.w3schools.com/react/react_native_flexbox.asp)
- [FreeCodeCamp: Flexbox in React Native](https://www.freecodecamp.org/news/flexbox-in-react-native/)

**Expected Output:**
For Example 1:
```
Displays a layout with three boxes arranged in a row using Flexbox properties for horizontal alignment.
```
For Example 2:
```
Displays a layout with three boxes arranged in a column using Flexbox properties for vertical alignment.
```

---

### Task 10: Study text input and buttons

**Explanation:**
Text input and buttons are fundamental components in React Native that allow users to interact with the application. The `TextInput` component is used to create input fields where users can enter text. The `Button` component is used to create clickable buttons that perform actions when pressed. These components are essential for creating forms, capturing user input, and triggering events.

Studying text input and buttons is crucial for building interactive and user-friendly applications in React Native. Understanding how to use these components, handle their events, and style them appropriately helps you create responsive and accessible user interfaces.

**Resources:**
- [React Native Official Documentation: TextInput](https://reactnative.dev/docs/textinput)
- [React Native Official Documentation: Button](https://reactnative.dev/docs/button)

**Example 1:**
```javascript
import React, { useState } from 'react';
import { View, TextInput, Button, Text, StyleSheet } from 'react-native';

function TextInputExample() {
  const [text, setText] = useState('');

  const handlePress = () => {
    alert(`You entered: ${text}`);
  };

  return (
    <View style={styles.container}>
      <TextInput
        style={styles.input}
        placeholder="Enter text"
        value={text}
        onChangeText={setText}
      />
      <Button title

="Submit" onPress={handlePress} />
      <Text style={styles.text}>You entered: {text}</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 16,
  },
  input: {
    width: '80%',
    padding: 8,
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 4,
    marginBottom: 16,
  },
  text: {
    marginTop: 16,
    fontSize: 16,
  },
});

export default TextInputExample;
```

**Example Explanation:**
This example demonstrates using the `TextInput` and `Button` components to create an input field and a submit button. The `TextInput` component captures user input, and the `Button` component triggers an alert displaying the entered text when pressed. The entered text is also displayed below the button.

**Example 2:**
```javascript
import React, { useState } from 'react';
import { View, TextInput, Button, Text, StyleSheet } from 'react-native';

function LoginForm() {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');

  const handleLogin = () => {
    alert(`Username: ${username}, Password: ${password}`);
  };

  return (
    <View style={styles.container}>
      <TextInput
        style={styles.input}
        placeholder="Username"
        value={username}
        onChangeText={setUsername}
      />
      <TextInput
        style={styles.input}
        placeholder="Password"
        value={password}
        onChangeText={setPassword}
        secureTextEntry
      />
      <Button title="Login" onPress={handleLogin} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 16,
  },
  input: {
    width: '80%',
    padding: 8,
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 4,
    marginBottom: 16,
  },
});

export default LoginForm;
```

**Example Explanation:**
This example demonstrates creating a login form with `TextInput` components for the username and password, and a `Button` component for submitting the form. The entered username and password are displayed in an alert when the button is pressed.

**Resources:**
- [W3Schools: React Native TextInput](https://www.w3schools.com/react/react_native_textinput.asp)
- [W3Schools: React Native Button](https://www.w3schools.com/react/react_native_button.asp)

**Expected Output:**
For Example 1:
```
Displays an input field and a button. The entered text is displayed in an alert and below the button when the button is pressed.
```
For Example 2:
```
Displays a login form with input fields for username and password and a login button. The entered username and password are displayed in an alert when the button is pressed.
```

---

### Task 11: Create forms with inputs and buttons

**Explanation:**
Creating forms with inputs and buttons in React Native involves using the `TextInput` and `Button` components to capture user input and trigger actions. Forms are essential for capturing user data, such as login credentials, registration details, and search queries. Handling form state, validating input, and submitting data are key aspects of creating forms.

Creating forms with inputs and buttons helps you build interactive and user-friendly applications. It allows you to capture and process user data, implement form validation, and provide a seamless user experience.

**Resources:**
- [React Native Official Documentation: Handling Text Input](https://reactnative.dev/docs/handling-text-input)
- [React Native Official Documentation: Button](https://reactnative.dev/docs/button)

**Example 1:**
```javascript
import React, { useState } from 'react';
import { View, TextInput, Button, Text, StyleSheet } from 'react-native';

function RegistrationForm() {
  const [formData, setFormData] = useState({ name: '', email: '', password: '' });

  const handleChange = (name, value) => {
    setFormData({ ...formData, [name]: value });
  };

  const handleSubmit = () => {
    alert(`Name: ${formData.name}, Email: ${formData.email}, Password: ${formData.password}`);
  };

  return (
    <View style={styles.container}>
      <TextInput
        style={styles.input}
        placeholder="Name"
        value={formData.name}
        onChangeText={(value) => handleChange('name', value)}
      />
      <TextInput
        style={styles.input}
        placeholder="Email"
        value={formData.email}
        onChangeText={(value) => handleChange('email', value)}
      />
      <TextInput
        style={styles.input}
        placeholder="Password"
        value={formData.password}
        onChangeText={(value) => handleChange('password', value)}
        secureTextEntry
      />
      <Button title="Register" onPress={handleSubmit} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 16,
  },
  input: {
    width: '80%',
    padding: 8,
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 4,
    marginBottom: 16,
  },
});

export default RegistrationForm;
```

**Example Explanation:**
This example demonstrates creating a registration form with `TextInput` components for name, email, and password, and a `Button` component for submitting the form. The entered data is displayed in an alert when the button is pressed.

**Example 2:**
```javascript
import React, { useState } from 'react';
import { View, TextInput, Button, Text, StyleSheet } from 'react-native';

function FeedbackForm() {
  const [feedback, setFeedback] = useState('');

  const handleSubmit = () => {
    alert(`Feedback: ${feedback}`);
  };

  return (
    <View style={styles.container}>
      <TextInput
        style={styles.input}
        placeholder="Enter your feedback"
        value={feedback}
        onChangeText={setFeedback}
      />
      <Button title="Submit" onPress={handleSubmit} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 16,
  },
  input: {
    width: '80%',
    padding: 8,
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 4,
    marginBottom: 16,
  },
});

export default FeedbackForm;
```

**Example Explanation:**
This example demonstrates creating a feedback form with a `TextInput` component for capturing feedback and a `Button` component for submitting the form. The entered feedback is displayed in an alert when the button is pressed.

**Resources:**
- [W3Schools: React Native Forms](https://www.w3schools.com/react/react_native_forms.asp)
- [FreeCodeCamp: Creating Forms in React Native](https://www.freecodecamp.org/news/react-native-forms/)

**Expected Output:**
For Example 1:
```
Displays a registration form with input fields for name, email, and password, and a register button. The entered data is displayed in an alert when the button is pressed.
```
For Example 2:
```
Displays a feedback form with an input field for feedback and a submit button. The entered feedback is displayed in an alert when the button is pressed.
```

---

### Task 12: Study ScrollView and FlatList

**Explanation:**
`ScrollView` and `FlatList` are two essential components in React Native for displaying scrollable content. `ScrollView` is a general-purpose scrolling container that can hold any number of components and views. `FlatList` is optimized for rendering large lists of data efficiently by recycling views that are no longer visible on the screen.

Studying `ScrollView` and `FlatList` helps you understand how to create scrollable content and efficiently manage large data sets in your React Native applications. These components are crucial for building responsive and performant user interfaces.

**Resources:**
- [React Native Official Documentation: ScrollView](https://reactnative.dev/docs/scrollview)
- [React Native Official Documentation: FlatList](https://reactnative.dev/docs/flatlist)

**Example 1:**
```javascript
import React from 'react';
import { ScrollView, View, Text, StyleSheet } from 'react-native';

function ScrollViewExample() {
  return (
    <ScrollView contentContainerStyle={styles.container}>
      {[...Array(20).keys()].map((i) => (
        <View key={i} style={styles.box}>
          <Text>Item {i + 1}</Text>
        </View>
      ))}
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  box: {
    backgroundColor: '#f0f0f0',
    padding: 16,
    marginBottom: 16,
    alignItems: 'center',
    borderRadius: 8,
  },
});

export default ScrollViewExample;
```

**Example Explanation:**
This example

 demonstrates using `ScrollView` to create a scrollable list of items. The `ScrollView` component wraps a list of `View` components, each displaying an item. The content is scrollable vertically.

**Example 2:**
```javascript
import React from 'react';
import { FlatList, View, Text, StyleSheet } from 'react-native';

const data = [...Array(20).keys()].map((i) => ({ key: `Item ${i + 1}` }));

function FlatListExample() {
  return (
    <FlatList
      data={data}
      renderItem={({ item }) => (
        <View style={styles.box}>
          <Text>{item.key}</Text>
        </View>
      )}
      keyExtractor={(item) => item.key}
      contentContainerStyle={styles.container}
    />
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  box: {
    backgroundColor: '#f0f0f0',
    padding: 16,
    marginBottom: 16,
    alignItems: 'center',
    borderRadius: 8,
  },
});

export default FlatListExample;
```

**Example Explanation:**
This example demonstrates using `FlatList` to create an efficient list of items. The `FlatList` component takes a data array and a `renderItem` function to render each item. The list is scrollable and optimized for performance.

**Resources:**
- [W3Schools: React Native ScrollView](https://www.w3schools.com/react/react_native_scrollview.asp)
- [W3Schools: React Native FlatList](https://www.w3schools.com/react/react_native_flatlist.asp)

**Expected Output:**
For Example 1:
```
Displays a scrollable list of items using the ScrollView component.
```
For Example 2:
```
Displays an efficient scrollable list of items using the FlatList component.
```

---

### Task 13: Implement scrollable content

**Explanation:**
Implementing scrollable content in React Native involves using components like `ScrollView` and `FlatList` to create views that can scroll vertically or horizontally. These components allow users to navigate through content that exceeds the visible screen area, providing a better user experience.

Implementing scrollable content is essential for displaying large datasets, long text, or collections of items in a way that is accessible and user-friendly. It helps you create responsive and interactive interfaces that adapt to different screen sizes.

**Resources:**
- [React Native Official Documentation: ScrollView](https://reactnative.dev/docs/scrollview)
- [React Native Official Documentation: FlatList](https://reactnative.dev/docs/flatlist)

**Example 1:**
```javascript
import React from 'react';
import { ScrollView, View, Text, StyleSheet } from 'react-native';

function ScrollableContent() {
  return (
    <ScrollView contentContainerStyle={styles.container}>
      {[...Array(30).keys()].map((i) => (
        <View key={i} style={styles.box}>
          <Text>Scrollable Item {i + 1}</Text>
        </View>
      ))}
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  box: {
    backgroundColor: '#e0e0e0',
    padding: 20,
    marginVertical: 10,
    borderRadius: 10,
    alignItems: 'center',
  },
});

export default ScrollableContent;
```

**Example Explanation:**
This example demonstrates creating scrollable content using `ScrollView`. The `ScrollView` component wraps a list of `View` components, each representing an item that can be scrolled vertically.

**Example 2:**
```javascript
import React from 'react';
import { FlatList, View, Text, StyleSheet } from 'react-native';

const data = [...Array(30).keys()].map((i) => ({ key: `Item ${i + 1}` }));

function ScrollableList() {
  return (
    <FlatList
      data={data}
      renderItem={({ item }) => (
        <View style={styles.box}>
          <Text>{item.key}</Text>
        </View>
      )}
      keyExtractor={(item) => item.key}
      contentContainerStyle={styles.container}
    />
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  box: {
    backgroundColor: '#e0e0e0',
    padding: 20,
    marginVertical: 10,
    borderRadius: 10,
    alignItems: 'center',
  },
});

export default ScrollableList;
```

**Example Explanation:**
This example demonstrates creating scrollable content using `FlatList`. The `FlatList` component renders a list of items efficiently, allowing for smooth scrolling and optimized performance.

**Resources:**
- [W3Schools: React Native ScrollView](https://www.w3schools.com/react/react_native_scrollview.asp)
- [W3Schools: React Native FlatList](https://www.w3schools.com/react/react_native_flatlist.asp)

**Expected Output:**
For Example 1:
```
Displays a scrollable list of items using the ScrollView component.
```
For Example 2:
```
Displays an efficient scrollable list of items using the FlatList component.
```

---

### Task 14: Learn about navigation with React Navigation

**Explanation:**
React Navigation is a popular library for handling navigation in React Native applications. It provides a comprehensive solution for managing navigation, including stack navigation, tab navigation, drawer navigation, and more. React Navigation allows you to navigate between screens, pass parameters, and handle deep linking.

Learning about navigation with React Navigation is crucial for building multi-screen applications with a seamless user experience. It helps you create complex navigation flows, handle user interactions, and manage screen transitions effectively.

**Resources:**
- [React Navigation Official Documentation](https://reactnavigation.org/docs/getting-started)
- [JavaScript.info: React Navigation](https://javascript.info/react-navigation)

**Example 1:**
```javascript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createStackNavigator } from '@react-navigation/stack';
import { Button, Text, View } from 'react-native';

const Stack = createStackNavigator();

function HomeScreen({ navigation }) {
  return (
    <View>
      <Text>Home Screen</Text>
      <Button
        title="Go to Details"
        onPress={() => navigation.navigate('Details')}
      />
    </View>
  );
}

function DetailsScreen() {
  return (
    <View>
      <Text>Details Screen</Text>
    </View>
  );
}

function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator initialRouteName="Home">
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Details" component={DetailsScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}

export default App;
```

**Example Explanation:**
This example demonstrates basic stack navigation using React Navigation. The `NavigationContainer` wraps the navigation structure, and the `Stack.Navigator` defines the navigation stack with two screens: Home and Details. The `HomeScreen` contains a button to navigate to the Details screen.

**Example 2:**
```javascript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
import { Text, View } from 'react-native';

const Tab = createBottomTabNavigator();

function HomeScreen() {
  return (
    <View>
      <Text>Home Screen</Text>
    </View>
  );
}

function SettingsScreen() {
  return (
    <View>
      <Text>Settings Screen</Text>
    </View>
  );
}

function App() {
  return (
    <NavigationContainer>
      <Tab.Navigator>
        <Tab.Screen name="Home" component={HomeScreen} />
        <Tab.Screen name="Settings" component={SettingsScreen} />
      </Tab.Navigator>
    </NavigationContainer>
  );
}

export default App;
```

**Example Explanation:**
This example demonstrates tab navigation using React Navigation. The `NavigationContainer` wraps the navigation structure, and the `Tab.Navigator` defines the tab navigation with two screens: Home and Settings.

**Resources:**
- [W3Schools: React Navigation](https://www.w3schools.com/react/react_native_navigation.asp)
- [FreeCodeCamp: React Navigation Guide](https://www.freecodecamp.org/news/react-navigation-guide/)

**Expected Output:**
For Example 1:
```
Displays a stack navigator with Home and Details screens. The Home screen contains a button to navigate to the Details screen.
```
For Example 2:
```
Displays a tab navigator with Home and Settings screens.
```

---

### Task 15: Set up basic navigation

**Explanation:**
Setting up basic navigation in React Native involves configuring the navigation container and defining navigation stacks or tabs. React Navigation provides different navigators like stack, tab, and drawer navigators to handle various navigation patterns. The `NavigationContainer` component wraps the entire navigation structure and provides context for navigation actions.

Setting up basic navigation is essential for creating multi-screen applications with a seamless user experience. It allows you to manage screen transitions, handle user interactions, and organize the flow of your application.

**Resources:**
- [React Navigation Official Documentation: Getting Started](https://reactnavigation.org/docs/getting-started)
- [JavaScript.info: React Navigation Basics](https://javascript.info/react-navigation/basics)

**Example 1:**
```javascript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native

';
import { createStackNavigator } from '@react-navigation/stack';
import { Button, Text, View } from 'react-native';

const Stack = createStackNavigator();

function HomeScreen({ navigation }) {
  return (
    <View>
      <Text>Home Screen</Text>
      <Button
        title="Go to Profile"
        onPress={() => navigation.navigate('Profile')}
      />
    </View>
  );
}

function ProfileScreen() {
  return (
    <View>
      <Text>Profile Screen</Text>
    </View>
  );
}

function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator initialRouteName="Home">
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Profile" component={ProfileScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}

export default App;
```

**Example Explanation:**
This example demonstrates setting up basic stack navigation with two screens: Home and Profile. The `NavigationContainer` wraps the navigation structure, and the `Stack.Navigator` defines the navigation stack. The Home screen contains a button to navigate to the Profile screen.

**Example 2:**
```javascript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
import { Text, View } from 'react-native';

const Tab = createBottomTabNavigator();

function HomeScreen() {
  return (
    <View>
      <Text>Home Screen</Text>
    </View>
  );
}

function SettingsScreen() {
  return (
    <View>
      <Text>Settings Screen</Text>
    </View>
  );
}

function App() {
  return (
    <NavigationContainer>
      <Tab.Navigator>
        <Tab.Screen name="Home" component={HomeScreen} />
        <Tab.Screen name="Settings" component={SettingsScreen} />
      </Tab.Navigator>
    </NavigationContainer>
  );
}

export default App;
```

**Example Explanation:**
This example demonstrates setting up basic tab navigation with two screens: Home and Settings. The `NavigationContainer` wraps the navigation structure, and the `Tab.Navigator` defines the tab navigation.

**Resources:**
- [W3Schools: React Navigation Basics](https://www.w3schools.com/react/react_native_navigation.asp)
- [FreeCodeCamp: Setting Up Basic Navigation in React Native](https://www.freecodecamp.org/news/react-navigation-guide/)

**Expected Output:**
For Example 1:
```
Displays a stack navigator with Home and Profile screens. The Home screen contains a button to navigate to the Profile screen.
```
For Example 2:
```
Displays a tab navigator with Home and Settings screens.
```

---

### Task 16: Create a stack navigator

**Explanation:**
A stack navigator in React Navigation is used to manage a stack of screens, where each new screen is pushed onto the stack and users can navigate back to the previous screen. Stack navigation is commonly used for workflows that involve moving forward and backward between screens, such as navigating from a list to a detail view.

Creating a stack navigator is essential for managing linear navigation flows in your application. It allows you to handle screen transitions, manage navigation history, and pass parameters between screens.

**Resources:**
- [React Navigation Official Documentation: Stack Navigator](https://reactnavigation.org/docs/stack-navigator)
- [JavaScript.info: Stack Navigation](https://javascript.info/react-navigation/stack)

**Example 1:**
```javascript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createStackNavigator } from '@react-navigation/stack';
import { Button, Text, View } from 'react-native';

const Stack = createStackNavigator();

function HomeScreen({ navigation }) {
  return (
    <View>
      <Text>Home Screen</Text>
      <Button
        title="Go to Details"
        onPress={() => navigation.navigate('Details')}
      />
    </View>
  );
}

function DetailsScreen() {
  return (
    <View>
      <Text>Details Screen</Text>
    </View>
  );
}

function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator initialRouteName="Home">
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Details" component={DetailsScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}

export default App;
```

**Example Explanation:**
This example demonstrates creating a stack navigator with two screens: Home and Details. The `NavigationContainer` wraps the navigation structure, and the `Stack.Navigator` defines the stack navigation. The Home screen contains a button to navigate to the Details screen.

**Example 2:**
```javascript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createStackNavigator } from '@react-navigation/stack';
import { Button, Text, View } from 'react-native';

const Stack = createStackNavigator();

function ProductScreen({ navigation }) {
  return (
    <View>
      <Text>Product Screen</Text>
      <Button
        title="Go to Cart"
        onPress={() => navigation.navigate('Cart')}
      />
    </View>
  );
}

function CartScreen() {
  return (
    <View>
      <Text>Cart Screen</Text>
    </View>
  );
}

function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator initialRouteName="Product">
        <Stack.Screen name="Product" component={ProductScreen} />
        <Stack.Screen name="Cart" component={CartScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}

export default App;
```

**Example Explanation:**
This example demonstrates creating a stack navigator with two screens: Product and Cart. The `NavigationContainer` wraps the navigation structure, and the `Stack.Navigator` defines the stack navigation. The Product screen contains a button to navigate to the Cart screen.

**Resources:**
- [W3Schools: Stack Navigator](https://www.w3schools.com/react/react_native_navigation.asp)
- [FreeCodeCamp: Creating a Stack Navigator in React Native](https://www.freecodecamp.org/news/react-navigation-guide/)

**Expected Output:**
For Example 1:
```
Displays a stack navigator with Home and Details screens. The Home screen contains a button to navigate to the Details screen.
```
For Example 2:
```
Displays a stack navigator with Product and Cart screens. The Product screen contains a button to navigate to the Cart screen.
```

---

### Task 17: Pass parameters between screens

**Explanation:**
Passing parameters between screens in React Navigation allows you to send data from one screen to another. This is useful for scenarios like passing user input, item details, or state information. Parameters can be passed using the `navigate` method and accessed using the `route` object on the destination screen.

Passing parameters between screens is essential for creating dynamic and interactive applications. It allows you to share data between screens, customize screen content based on parameters, and handle user interactions effectively.

**Resources:**
- [React Navigation Official Documentation: Passing Parameters](https://reactnavigation.org/docs/params)
- [JavaScript.info: Passing Parameters in React Navigation](https://javascript.info/react-navigation/params)

**Example 1:**
```javascript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createStackNavigator } from '@react-navigation/stack';
import { Button, Text, View } from 'react-native';

const Stack = createStackNavigator();

function HomeScreen({ navigation }) {
  return (
    <View>
      <Text>Home Screen</Text>
      <Button
        title="Go to Details"
        onPress={() => navigation.navigate('Details', { itemId: 42 })}
      />
    </View>
  );
}

function DetailsScreen({ route }) {
  const { itemId } = route.params;
  return (
    <View>
      <Text>Details Screen</Text>
      <Text>Item ID: {itemId}</Text>
    </View>
  );
}

function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator initialRouteName="Home">
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Details" component={DetailsScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}

export default App;
```

**Example Explanation:**
This example demonstrates passing parameters between screens in a stack navigator. The Home screen passes an `itemId` parameter to the Details screen using the `navigate` method. The Details screen accesses the parameter using the `route` object.

**Example 2:**
```javascript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createStackNavigator } from '@react-navigation/stack';
import { Button, Text, View } from 'react-native';

const Stack = createStackNavigator();

function ProductScreen({ navigation }) {
  return (
    <View>
      <Text>Product Screen</Text>
      <Button
        title="Go to Cart"
        onPress={() => navigation.navigate('Cart', { productId: 123 })}
      />
    </View>
  );
}

function CartScreen({ route }) {
  const { productId } = route.params;
  return (
    <View>
      <Text>Cart Screen</Text>
      <Text>Product ID: {productId}</Text>
    </View>
  );
}

function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator initialRouteName="Product">
        <Stack.Screen name="Product" component={ProductScreen} />
        <Stack.Screen name="Cart" component={CartScreen} />
      </Stack.Navigator>
    </NavigationContainer>
 

 );
}

export default App;
```

**Example Explanation:**
This example demonstrates passing parameters between screens in a stack navigator. The Product screen passes a `productId` parameter to the Cart screen using the `navigate` method. The Cart screen accesses the parameter using the `route` object.

**Resources:**
- [W3Schools: Passing Parameters](https://www.w3schools.com/react/react_native_navigation.asp)
- [FreeCodeCamp: Passing Parameters in React Navigation](https://www.freecodecamp.org/news/react-navigation-guide/)

**Expected Output:**
For Example 1:
```
Displays a stack navigator with Home and Details screens. The Home screen passes an itemId parameter to the Details screen, which displays the itemId.
```
For Example 2:
```
Displays a stack navigator with Product and Cart screens. The Product screen passes a productId parameter to the Cart screen, which displays the productId.
```

---

### Task 18: Study state management in React Native

**Explanation:**
State management in React Native involves managing the state of components and the application as a whole. The `useState` hook is used to manage local state within functional components, while the `useEffect` hook is used for side effects like fetching data or updating the DOM. For global state management, libraries like Redux or Context API can be used to share state across the application.

Studying state management is crucial for building dynamic and interactive applications. It helps you manage component state, handle user interactions, and update the UI based on state changes.

**Resources:**
- [React Native Official Documentation: State](https://reactnative.dev/docs/state)
- [JavaScript.info: React Native State Management](https://javascript.info/react-native/state)

**Example 1:**
```javascript
import React, { useState } from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

function Counter() {
  const [count, setCount] = useState(0);

  return (
    <View style={styles.container}>
      <Text style={styles.text}>Count: {count}</Text>
      <Button title="Increment" onPress={() => setCount(count + 1)} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 24,
    marginBottom: 16,
  },
});

export default Counter;
```

**Example Explanation:**
This example demonstrates using the `useState` hook to manage the state of a counter component. The `count` state is initialized to 0, and the `setCount` function is used to update the state when the button is pressed.

**Example 2:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet } from 'react-native';

function Timer() {
  const [seconds, setSeconds] = useState(0);

  useEffect(() => {
    const interval = setInterval(() => {
      setSeconds((prevSeconds) => prevSeconds + 1);
    }, 1000);
    return () => clearInterval(interval);
  }, []);

  return (
    <View style={styles.container}>
      <Text style={styles.text}>Seconds: {seconds}</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 24,
  },
});

export default Timer;
```

**Example Explanation:**
This example demonstrates using the `useEffect` hook to manage side effects in a timer component. The `seconds` state is updated every second using a setInterval function. The effect cleans up the interval when the component unmounts.

**Resources:**
- [W3Schools: React Native State Management](https://www.w3schools.com/react/react_native_state.asp)
- [FreeCodeCamp: State Management in React Native](https://www.freecodecamp.org/news/react-native-state-management/)

**Expected Output:**
For Example 1:
```
Displays a counter with a button to increment the count using the useState hook.
```
For Example 2:
```
Displays a timer that increments the seconds count every second using the useEffect hook.
```

---

### Task 19: Use `useState` and `useEffect` hooks

**Explanation:**
The `useState` hook is used to manage local state in functional components. It returns an array with the current state value and a function to update it. The `useEffect` hook is used to manage side effects, such as fetching data, updating the DOM, or setting up subscriptions. It takes a function and an optional array of dependencies that determine when the effect should run.

Using `useState` and `useEffect` hooks is essential for managing state and side effects in React Native applications. These hooks allow you to create dynamic and interactive components that respond to user interactions and external data changes.

**Resources:**
- [React Native Official Documentation: Hooks](https://reactnative.dev/docs/hooks-intro)
- [JavaScript.info: React Hooks](https://javascript.info/react/hooks)

**Example 1:**
```javascript
import React, { useState } from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

function Toggle() {
  const [isOn, setIsOn] = useState(false);

  return (
    <View style={styles.container}>
      <Text style={styles.text}>The switch is {isOn ? 'ON' : 'OFF'}</Text>
      <Button title="Toggle" onPress={() => setIsOn(!isOn)} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 24,
    marginBottom: 16,
  },
});

export default Toggle;
```

**Example Explanation:**
This example demonstrates using the `useState` hook to manage the state of a toggle component. The `isOn` state is toggled between true and false when the button is pressed.

**Example 2:**
```javascript
import React, { useState, useEffect } from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

function Countdown() {
  const [seconds, setSeconds] = useState(10);

  useEffect(() => {
    if (seconds > 0) {
      const interval = setInterval(() => {
        setSeconds((prevSeconds) => prevSeconds - 1);
      }, 1000);
      return () => clearInterval(interval);
    }
  }, [seconds]);

  return (
    <View style={styles.container}>
      <Text style={styles.text}>Countdown: {seconds}</Text>
      {seconds === 0 && <Button title="Reset" onPress={() => setSeconds(10)} />}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 24,
    marginBottom: 16,
  },
});

export default Countdown;
```

**Example Explanation:**
This example demonstrates using the `useEffect` hook to manage side effects in a countdown timer. The `seconds` state is decremented every second using a setInterval function. The effect cleans up the interval when the component unmounts or the `seconds` value changes.

**Resources:**
- [W3Schools: React Native Hooks](https://www.w3schools.com/react/react_native_hooks.asp)
- [FreeCodeCamp: Using Hooks in React Native](https://www.freecodecamp.org/news/react-hooks-tutorial/)

**Expected Output:**
For Example 1:
```
Displays a toggle switch that changes between ON and OFF using the useState hook.
```
For Example 2:
```
Displays a countdown timer that decrements every second using the useEffect hook. When the countdown reaches zero, a reset button appears.
```

---

### Task 20: Build a simple multi-screen app

**Explanation:**
Building a simple multi-screen app in React Native involves using navigation to transition between multiple screens and manage the state and data flow between them. This typically includes setting up a navigation structure, creating screens, passing parameters, and handling user interactions.

Building a simple multi-screen app helps you understand how to structure your application, manage navigation, and create a seamless user experience. It allows you to practice integrating different components, handling state, and passing data between screens.

**Resources:**
- [React Navigation Official Documentation](https://reactnavigation.org/docs/getting-started)
- [JavaScript.info: Multi-Screen App in React Native](https://javascript.info/react-navigation/multi-screen)

**Example:**
```javascript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createStackNavigator } from '@react-navigation/stack';
import { Button, Text, View } from 'react-native';

const Stack = createStackNavigator();

function HomeScreen({ navigation }) {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Home Screen</Text>
      <Button
        title="Go to Profile"
        onPress={() => navigation.navigate('Profile', { userId: 123 })}
      />
    </View>
  );
}

function ProfileScreen({ route }) {
  const { userId } = route.params;
  return (
    <View style={styles.container}>
      <Text style={styles

.text}>Profile Screen</Text>
      <Text style={styles.text}>User ID: {userId}</Text>
      <Button title="Go to Details" onPress={() => navigation.navigate('Details')} />
    </View>
  );
}

function DetailsScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Details Screen</Text>
    </View>
  );
}

const styles = {
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 20,
    marginBottom: 20,
  },
};

function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator initialRouteName="Home">
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Profile" component={ProfileScreen} />
        <Stack.Screen name="Details" component={DetailsScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}

export default App;
```

**Example Explanation:**
This example demonstrates building a simple multi-screen app with three screens: Home, Profile, and Details. The `NavigationContainer` wraps the navigation structure, and the `Stack.Navigator` defines the stack navigation. The Home screen contains a button to navigate to the Profile screen, passing a `userId` parameter. The Profile screen displays the user ID and a button to navigate to the Details screen.

**Resources:**
- [W3Schools: Multi-Screen App](https://www.w3schools.com/react/react_native_navigation.asp)
- [FreeCodeCamp: Building a Multi-Screen App in React Native](https://www.freecodecamp.org/news/react-navigation-guide/)

**Expected Output:**
```
Displays a simple multi-screen app with Home, Profile, and Details screens, demonstrating navigation and parameter passing between screens.
```
---
